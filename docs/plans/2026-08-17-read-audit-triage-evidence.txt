
===== CLUSTER: backend-amnesia =====
--- S0-guard [OPEN] effort:S
CLAIM: hasSqliteState tests twining.db existence not size, so a 0-byte db resolves backend to sqlite and silently boots an empty database beside real v1 legacy decisions/, violating the module's own documented safe-direction rule.
EVIDENCE: src/storage/backend-resolve.ts:36 — `if (fs.existsSync(path.join(twiningDir, "twining.db"))) return true;` — existence only, no size or header check, exactly as the report describes | src/storage/backend-resolve.ts:9-15 — docblock states the broken invariant: 'the flip must never boot an empty database next to real legacy state (silent amnesia)' and 'misdetection must land on files (safe), never on an empty database' | test/backend-resolve.test.ts:94-98 — a current test writes a 0-byte twining.db beside non-empty blackboard.jsonl and asserts {backend: sqlite, reason: sqlite-state}, i.e. the dangerous resolution is test-enshrined at HEAD (also 45-46)
FIX: In hasSqliteState (backend-resolve.ts:36), replace existsSync with fs.statSync(dbPath).size > 0 — or stronger, read the first 16 bytes and require the 'SQLite format 3\0' header. Leave the records/ clause untouched: a 0-byte db with populated records/ still resolves sqlite via that clause and rehydrates (report arm 1, safe). Rewrite test/backend-resolve.test.ts:45-46 and 94-98 to use a non-empty db, and add a regression test: 0-byte db + legacy decisions/ content → {files, legacy-content}.
RISK: Behavior change for any store currently in the amnesia state: next boot flips it from empty-sqlite to files backend with the migrate nudge — that is the intended rescue but sessions will suddenly see hundreds of decisions reappear. A crash-created 0-byte db on a truly fresh store (no legacy content) resolves 'fresh'→sqlite, unchanged. Header-check variant additionally routes size>0-but-corrupt dbs to files per the ambiguity rule.
--- S0-explicit [OPEN] effort:S
CLAIM: The same amnesia reproduces with config backend: sqlite set explicitly when records/ is empty and content lives only in v1 legacy decisions/ — the sqlite backend cannot read the legacy tier and nothing warns.
EVIDENCE: src/storage/backend-factory.ts:80-82 — explicit config skips resolveAutoBackend entirely (`requested = configured`), so no legacy detection runs on that path | src/storage/backend-factory.ts:84-155 — the sqlite branch opens the db and ingests only .twining/records/ (line 104); it never inspects or mentions the legacy decisions/ tier; the only adjacent warning is the export_records:false stale-tree notice (120-126), a different condition | src/storage/backend-resolve.ts:52-97 — hasLegacyContent exists but is module-private and is only consulted on the auto path
FIX: Export hasLegacyContent from backend-resolve.ts. In createStores' sqlite branch (backend-factory.ts, after openDatabase/ingest), if the decisions table is empty AND hasLegacyContent(twiningDir), console.error a loud notice naming `npx twining-mcp migrate`, and set a flag on StoreSet (e.g. legacy_content_unread: true) so twining_status can carry it as a warning (see S0-surface-backend). Warn-only is the conservative default; auto-migrating under an explicit backend stamp is the owner's design call — the report itself poses it as a question.
RISK: False-positive warnings on repos that legitimately hold retired legacy files — bounded by gating on empty-db (post-migrate repos have a populated db and never warn). Auto-migrate variant would surprise-mutate tracked config.yml, which src/migrate/auto.ts:5-9 explicitly rejects as the default posture.
--- S0-warning-inverted [OPEN] effort:S
CLAIM: backend-factory warns only on reason legacy-content (the safe files-backend path); the dangerous sqlite-state resolution beside unread legacy content is completely silent.
EVIDENCE: src/storage/backend-factory.ts:73-79 — `if (resolution.reason === "legacy-content")` is the ONLY notice; sqlite-state and fresh return with no output, exactly the inversion the report measured (arm B warns, arm A is silent) | grep 'sqlite-state' across src/ — only backend-resolve.ts and backend-factory.ts; no other code path warns on that resolution | src/tools/lifecycle-tools.ts:102-165 — status warnings cover stale provisionals, archivable entries, orphan entities only; nothing backend-related, so 'Healthy' with 0 decisions is still what an amnesia store reports
FIX: Same seam as S0-explicit: whenever the sqlite backend is selected (auto reason sqlite-state OR explicit) and the opened db has zero decisions while hasLegacyContent(twiningDir) is true, emit a stderr warning ('sqlite backend selected but N legacy decision files are unread — run twining-mcp migrate') and surface it through the StoreSet flag into twining_status warnings. This complements the S0-guard size fix rather than replacing it: the guard fixes auto-resolution; the warning also covers explicit-sqlite stores and any future empty-db-beside-legacy state.
RISK: Noise on migrated repos that keep legacy files as history — eliminated by the empty-db gate (their db is populated). Stderr-only warnings are known to be missed by agents (the report's whole point), so the status-warning half is the load-bearing part.
--- S0-surface-backend [OPEN] effort:S
CLAIM: No tool response carries the resolved backend or resolution reason anywhere, so twining_status reports 'Healthy' with 0 decisions during amnesia and an agent cannot learn which backend was chosen.
EVIDENCE: src/tools/lifecycle-tools.ts:167-180 — twining_status response fields are project, blackboard_entries, active_decisions, provisional_decisions, graph_entities, graph_relations, registered_agents, active_agents, last_activity, needs_archiving, warnings, summary — no backend field | src/server.ts:81,90 — `backend` IS destructured from createStores but used only for a fallback-log comparison; it is never passed to any tool registrar | src/storage/backend-factory.ts:49-62 — StoreSet carries `backend` but not the resolution reason; createStores discards `resolution.reason` after the notice check (line 71-79)
FIX: Add `reason: "sqlite-state" | "legacy-content" | "fresh" | "explicit" | "fallback"` to StoreSet in backend-factory.ts (the value already exists at line 71, just retain it; stamp 'fallback' in the catch at 156-163). Thread backend+reason from server.ts:89 into registerLifecycleTools and add `backend` and `backend_reason` to the twining_status response object at lifecycle-tools.ts:167; optionally append '(sqlite backend)' to assemble's status summary line. Minimal viable slice for the report's ask: status only.
RISK: Lowest-risk item in the cluster — purely additive response fields, trivial token cost; existing status assertions in test/ may need the new fields added. Putting it in every response (report's 'ideally') touches the shared toolResult path and budget baselines — do status first.
--- S0-index-desync [OPEN] effort:M
CLAIM: The v1 files backend is index.json-driven and silently under-reports decisions present on disk but missing from the index (4/770 measured); only the offline migrate CLI salvages them.
EVIDENCE: src/storage/decision-store.ts:193-211 — getIndex reads decisions/index.json only (with mtime cache); no directory scan, so every runtime read path (status counts, why, list) derives from the index and cannot see orphaned decision files | src/migrate/forward.ts:173-204 — orphan salvage ('salvaged N decision(s) present on disk but missing from decisions/index.json (index desync)') exists only in the migrate CLI; shipped 1.24.0 (commit dfd6add), i.e. it pre-dates the audit and is exactly what the report already credits | src/storage/backend-resolve.ts:71-75 — stray decision files count as legacy content for backend RESOLUTION only (shipped v2.0.0-beta.1, commit 75b67af); prevents desync stores reading as 'fresh' but does nothing for runtime under-reporting
FIX: Report-first, repair-behind-execute: add a files-backend desync check — readdir(decisionsDir) for `*.json` (excluding index.json), diff against index ids, cache by directory mtime. Surface a nonzero count as a twining_status warning ('N decision files on disk are missing from the index — run twining_housekeeping({repair_index: true, execute: true}) or twining-mcp migrate'). The housekeeping execute path appends salvaged entries to index.json under the existing index lock, reusing forward.ts's salvage/parse logic extracted into a shared helper.
RISK: Directory scan on every status call is O(files) on 770+-file stores — the mtime cache bounds it. Auto-repair writes index.json (a tracked file) from what agents treat as a read path — that collides directly with S4-14, which is why detection must default to report-only. Salvaged entries must round-trip the same field mapping migrate uses or index/file diverge in a new way.
--- S4-14-readonly [OPEN] effort:M
CLAIM: A single twining_status call mutates the store: storage/init reconciles the git-tracked .twining/.gitignore on every startup, metrics.jsonl is appended per tool call, and the sqlite open creates WAL files — and there is no read-only mode.
EVIDENCE: src/storage/init.ts:68-72 — existing-store path calls reconcileGitignore on EVERY startup; :53-58 appends missing canonical entries to .twining/.gitignore, a tracked file (idempotent — line 54 returns early when nothing is missing, matching the report's own bounding nuance) | src/server.ts:245-248 — MetricsCollector instruments every tool call, on by default (`config.analytics?.metrics?.enabled !== false`), appending metrics.jsonl on any status call | src/storage/backend-factory.ts:89 — openDatabase on any sqlite boot creates twining.db-wal/-shm as a side effect
FIX: Two independent halves. (a) Defer .gitignore reconciliation off the pure-read startup path: call reconcileGitignore from the write seam (first store write / ensureInitialized's write callers) instead of unconditionally in initTwiningDir — this alone removes the only TRACKED-file mutation. (b) Add TWINING_READ_ONLY=1 handled in server startup: skip initTwiningDir side effects, skip MetricsCollector instrumentation, reuse the existing enterReadOnlyMode machinery to reject write tools, open sqlite with node:sqlite's readOnly open option (no WAL), and skip record ingest. Half (a) is small and safe to ship alone; (b) is the full audit-mode ask.
RISK: Deferring gitignore reconcile re-opens the #44 field casualty (.last-record: 137 commits of churn) if any write can create an ignorable file before reconcile runs — reconcile must be ordered before the first write, not merely moved later. Read-only sqlite open fails its purpose on a store whose records/ tree is ahead of the db (ingest normally converges it) — read-only mode must accept documented staleness there. Skipping metrics changes dashboard usage data for such sessions.
NOTES: All six items verified by direct source read at HEAD (main, 2.15.0) — none of the 2.7.0-2.15.0 waves touched this cluster's surface, and none of these appear as named backlog items in docs/field-responses/ (checked all four memos), TWINING-DESIGN-SPEC.md, or CHANGELOG.md, so nothing qualifies as ALREADY_TRACKED. Orchestrator's ground truth confirmed: backend-resolve.ts:36 is existence-only. Two findings worth flagging beyond the classifications: (1) test/backend-resolve.test.ts:45-46 and 94-98 actively enshrine the 0-byte-db→sqlite behavior ('sqlite state wins over legacy content' is asserted with an EMPTY db file), so the S0-guard fix is a deliberate, test-visible behavior change, not a patch — those tests must be rewritten with a non-empty db in the same commit. (2) The cluster's fixes share one seam: exporting hasLegacyContent and adding backend+reason (+ a legacy_content_unread flag) to StoreSet serves S0-explicit, S0-warning-inverted, and S0-surface-backend in a single small change to backend-factory.ts, with lifecycle-tools.ts consuming it — I'd sequence S0-guard (with test rewrite) first, then that combined StoreSet change, then the two M items. Report sections read: lines 84-167 (S0 incl. its two self-corrections and one refutation — none of the surviving claims were retracted), 810 (S4-14), 836-842 (asks 0b/0c). Per hard rules I made no writes, ran no builds/tests, and did not start a server; the twining Gate 1/Gate 2 calls were deliberately skipped because this was an orchestrator-scoped read-only triage and twining_assemble itself mutates the store (the very defect in S4-14) — the parent orchestrator owns the record for this run.

===== CLUSTER: distribution =====
--- DIST-npm-lag [FIELD_ENV_ONLY] effort:S
CLAIM: The npm-published line was behind the plugin bundle (2.6.0 vs 2.13.0) so every npx session got old code; publish >=2.13.0 to npm.
EVIDENCE: npm view twining-mcp time: 2.7.0 published 2026-08-12T23:23Z, 2.12.0 2026-08-14T04:09Z, 2.13.0 2026-08-14T23:39Z — on the audit date (08-15) npm latest was 2.13.0, EQUAL to the plugin-1.31.0 bundle; the registry was not behind | npm view twining-mcp time: 2.14.0 2026-08-16T14:01Z, 2.15.0 2026-08-16T23:29Z — publishes tracked tags same-day through the whole 2.7–2.15 wave; there was no publish lag to fix, and the report's ask 0a ('publish >=2.13.0') was already satisfied ~9h before the report's date | plugin/scripts/launch-server.sh:42 — PKG_SPEC="twining-mcp@^2.0.0" (floating range, not a pin)
FIX: Nothing to publish — repo side was current. Optional docs: a README/field-memo troubleshooting note that under a registry minimumReleaseAge policy or offline cache the npx rung resolves an old server for the policy window (self-heals when the window passes), plus the one-call self-test the report proposes. NOTE: the 2026-08-16 memo's 'upgrade the plugin' instruction does not fix rung-1 sessions under an age policy — the bundle only serves when npx fails; worth adding to the memo.
RISK: Docs-only; no runtime risk.
--- DIST-bundle-embeddings [DESIGN_DECISION_OPEN] effort:M
CLAIM: The plugin bundle does not ship @huggingface/transformers, so forcing the bundle disables semantic search (fallback_mode true); ship the dep or make the tradeoff explicit.
EVIDENCE: scripts/build-plugin-bundle.mjs:80 — external: ["@huggingface/transformers", "posthog-node", "open"]; header lines 13-18 document it as deliberate: 'Externalized (loaded dynamically at runtime with graceful fallback) … keyword fallback' | src/embeddings/embedder.ts:119-135 — dynamic import('@huggingface/transformers') inside try/catch, sets fallbackMode=true on failure (the ERR_MODULE_NOT_FOUND path the report hit) | Tradeoff is already explicit in three places: README.md:195 ('On this rung semantic search degrades to keyword mode, announced with a one-line stderr notice'), launch-server.sh:242 and :210-212 (both bundle paths print 'Semantic search runs in keyword-fallback mode'), CHANGELOG.md:339 (tarball-bundle entry documents graceful degradation) — plus fallback_mode:true in every search response
FIX: Owner call, as the report itself frames it. Shipping the dep inside the committed single-file bundle is infeasible (onnxruntime-node native per-platform binaries). Viable middle path: make the embedder, when running from the bundle, also try createRequire against the project root so 'npm i -D @huggingface/transformers' in a project restores semantic mode under the bundle; plus add the keyword-mode stderr notice to the override rung (2 lines in launch-server.sh).
RISK: Loading a project-installed transformers risks API-version drift against the bundled embedder code; override-rung notice is risk-free.
--- DIST-version-self-report [OPEN] effort:S
CLAIM: A session cannot tell which server build serves it (two builds served concurrent sessions on one machine); add a version stamp to tool responses, e.g. twining_status returns server_version.
EVIDENCE: src/tools/lifecycle-tools.ts:167-179 — twining_status payload is project/blackboard_entries/active_decisions/…/summary; no version field, and grep 'version' across src/tools/*.ts finds none in any tool response | src/server.ts:236 — version: PKG_VERSION appears only in the McpServer serverInfo (initialize handshake), which Claude Code does not surface to the model; PKG_VERSION is consumed nowhere else | plugin/scripts/launch-server.sh:185 + plugin/hooks/session-start-context.sh:89-100 — the probe contract reports runner= and node= only, never the server version, so even the SessionStart context cannot say which build will serve
FIX: Export PKG_VERSION from src/server.ts (or a shared build-constants module using the existing __TWINING_VERSION__ define), pass it into registerLifecycleTools, and add server_version to the twining_status toolResult; optionally also stamp twining_assemble's status block and the dashboard /api/health. This also makes the 08-16 memo's verification step actually executable.
RISK: Response-shape additions may touch token-budget measurements and any tests asserting exact status payload keys; additive field, so near-zero breakage.
--- DIST-rung-order [DESIGN_DECISION_OPEN] effort:M
CLAIM: The launcher prefers npx over the bundle, so npm lag means an old server wins; is a rung-order change or min-version check warranted now that npm is current?
EVIDENCE: plugin/scripts/launch-server.sh:140-180 — rung order at HEAD is override > pin > npx > npm-prefix > global > bundled, unchanged since 2026-08-04 (git log: 5dee214 last touch) | launch-server.sh:242 + CHANGELOG.md:334 — npx-over-bundle is deliberate: the bundle is keyword-only, a registry-resolved install has full semantic mode; the report itself withdraws bundle-first as 'strictly worse' (its lines 388-399) | No skew today: npm latest 2.15.0 (published 2026-08-16T23:29Z) == plugin 1.33.0 bundle (plugin/server/twining-server.mjs carries "2.15.0"; plugin/.claude-plugin/plugin.json 1.33.0)
FIX: Design options for the owner: (a) stamp PKG_SPEC to the exact server version at each plugin release via bump-plugin-version.sh (and README.md:182's Windows line) — rung 1 then serves exactly the shipped version, and under a minimumReleaseAge policy the exact-pin resolution fails fast into the existing grace fallback (launch-server.sh:204-216): right version in keyword mode instead of wrong version in semantic mode; (b) keep the range but add a post-launch version floor — impractical, the launcher cannot cheaply learn the resolved version before exec; (c) bundle-first — already rejected by the report's own measurement. (a) is the coherent mechanism and pairs with DIST-version-self-report for verification.
RISK: Exact pin invalidates the npx cache every plugin release (one download per machine per release) and, under age policies, shifts sessions to keyword-mode bundle for the policy window — a deliberate availability-vs-freshness trade that must be announced to the field.
NOTES: Registry timeline settles the cluster's factual core: npm was NOT behind the bundle on 2026-08-15 (both were 2.13.0); the audit machine's npx resolving 2.6.0 is field-side — most parsimoniously the same minimumReleaseAge-class registry policy already documented in CHANGELOG.md:299 (a ~7-day window on 08-15 resolves exactly 2.6.0), or offline packument fallback (the report notes npm view was not runnable there). npm 9+ npx re-resolves range specs online per launch (libnpmexec missingFromTree, preferOnline:true), so healthy field machines self-heal; policy-gated ones stay stuck for the window regardless of plugin upgrades — which also means the 2026-08-16 memo's upgrade instruction alone may not move their rung-1 sessions off 2.6.0-class servers until the window passes. The report's 'unexplained' 08-14 amend-on-bundle session is explained at HEAD by the launcher's grace fall-through (launch-server.sh:204-216). Cross-item tie: memo-send.md:62 instructs the field to verify via a twining_status version report that does not exist — fixing DIST-version-self-report (S effort) is the cheapest, highest-leverage change in this cluster and unblocks verifying everything else.
CHALLENGE DIST-npm-lag -> CONFIRMED NOTE: Verdict and fix_sketch stand, including its correct memo caveat (upgrading the plugin does not fix rung-1 sessions under an age policy). Two guard-rails for the remediation plan: (1) soften the age-policy attribution to 'most consistent hypothesis' — CHANGELOG.md:299 names it as one of four candidate causes, not the proven one, and offline/proxy degradation to a cached packument fits the same observations; (2) ask 0a's conjoined repo-side requirement — the bundle ships without @huggingface/transformers, so any bundle fallback is keyword-only (launch-server.sh:212 says so at fallback time) — is outside this item's claim text and MUST be carried by another cluster item; if no other item covers it, this classification under-scopes the plan. Minor: the triage's '~9h before the report's date' arithmetic is loose (2.13.0 landed 08-14T23:39Z, ~16-23h before the 15:24 audit depending on timezone) but directionally correct.

===== CLUSTER: assemble =====
--- ASM-budget-order [OPEN] effort:M
CLAIM: assemble fills one shared budget with warnings first and decisions effectively last, so warning-dense scopes report decisions_count 0; ask is a reserved decisions floor or reorder.
EVIDENCE: src/engine/context-assembler.ts:303-340 — warnings fill FIRST from the FULL budget, degrading to summary-only before being omitted; warning-dense scopes can consume the entire budget | src/engine/context-assembler.ts:307-308 — the only reserve is FOR warnings (nonWarningCap = budget - 10%); there is no floor for decisions anywhere | src/engine/context-assembler.ts:342-353 — decisions compete score-ranked inside the leftover non-warning pool; if warnings exhausted the budget they get zero
FIX: In context-assembler.ts step 7 (~line 303), reserve a decisions floor (e.g. decisionsReserve = 15-20% of budget): fill warnings from budget - decisionsReserve, then guarantee top-scored decisions at least the reserved slice before the general non-warning fill. Keep the existing warnings_omitted disclosure so displaced warnings stay visible. Note the report's literal order model (warnings→needs→continue-work→decisions) is not the code's — handoffs are appended unbudgeted (lines 578-598) and needs get a safety pass (356-365) — but the starvation claim itself is confirmed. Ask 19 (document the token_estimate≈max_tokens truncation signature + large-max_tokens workaround in the tool description at context-tools.ts:20) is a docs-only S companion that ships the caller-side cure immediately.
RISK: Changes briefing composition on warning-dense scopes: some warnings drop from full text to summary-only/omitted. Warning-first was a deliberate design ('what not to do is the most actionable signal', line 703), so the floor must be small enough not to invert it.
--- ASM-disclosure [PARTIAL fixed_in: partial half: 2.6.0 (warnings_omitted), 2.7.0 (archived_excluded_count), 2.9.0 (superseded_excluded_count)] effort:S
CLAIM: assemble lacks why-style disclosure fields (total_in_scope, active_count, omitted_count, truncated); ask is to port them.
EVIDENCE: src/tools/context-tools.ts:47-61 — tool metadata at HEAD: decisions_count, warnings_count, needs_count, archived_excluded_count, superseded_excluded_count, token_estimate; no total_in_scope, no decisions-omitted count, no truncated flag | src/engine/context-assembler.ts:527-538 — AssembledContext carries warnings_omitted, archived_excluded_count, superseded_excluded_count only; warnings_omitted is rendered in briefing text (lines 695, 881) but never surfaced in the metadata JSON | src/engine/context-assembler.ts:112-115 — the scope population (activeDecisions from getByScope) IS computed and then dropped, so the missing fields are one plumbing step away
FIX: Add decisions_in_scope (= activeDecisions.length at context-assembler.ts:113), decisions_omitted (= in-scope active/provisional not in selected), and truncated:boolean to AssembledContext; expose them plus the existing warnings_omitted in the toolResult at context-tools.ts:47. Define explicitly whether the count is active-only or active+provisional so the S1-B ambiguity doesn't recur.
RISK: Minimal — purely additive response fields. Only hazard is semantic drift between decisions_in_scope and why's total_in_scope if the status filters differ; align both on the same predicate.
--- ASM-false-empty [OPEN] effort:S
CLAIM: assemble prints the affirmative 'No active decisions for this scope.' without verifying the scope population is 0; ask is to only emit it when verified, else say omitted-for-budget.
EVIDENCE: src/engine/context-assembler.ts:779-781 — 'No active decisions for this scope.' is emitted whenever the SURFACED set is empty, with no population check | src/engine/context-assembler.ts:326-353 — budget starvation empties the surfaced set while getByScope-population is non-zero, so the sentence prints against real populations | src/engine/context-assembler.ts:841-851 — the D3/D10 exclusion notes cover only archived/superseded statuses, not budget/selection omissions of ACTIVE decisions — that hole is exactly this finding
FIX: With ASM-disclosure's decisions_in_scope plumbed into AssembledContext, gate line 780: emit the affirmative sentence only when decisions_in_scope === 0 (and excluded counts are 0); otherwise emit 'N in-scope decision(s) did not fit the token budget — call twining_why on this scope.' Mirror the existing warnings_omitted directive pattern at line 695.
RISK: None material; depends on the ASM-disclosure plumbing landing first.
--- ASM-population [OPEN] effort:S
CLAIM: assemble's decisions are relevance/graph-weighted and reach outside the requested scope, so decisions_count is not a scope count and can exceed why's population; ask is rename/document or make scope-exact.
EVIDENCE: src/engine/context-assembler.ts:129-167 — semantic path searches ALL active/provisional decisions store-wide and merges any search hit into the candidate map with NO relevance floor (the SEMANTIC_ADMISSION_FLOOR at line 210 gates blackboard entries only, not decisions) | src/engine/context-assembler.ts:252-274 — merged candidates are scored by recency/relevance/confidence/graph-reachability; off-scope decisions are only dampened (scopeProximity 0.4, line 1253), never excluded | src/tools/context-tools.ts:50 — decisions_count = surfaced merged set size; grep confirms decisions_count appears nowhere else in src/plugin/docs, so its semantics are documented nowhere
FIX: Cheapest honest fix: keep decisions_count but document it in the tool description as 'decisions surfaced into the briefing (scope-matched plus task-relevant from elsewhere), not a scope census — use twining_why total_in_scope for populations', and ship decisions_in_scope alongside (ASM-disclosure). Prefer additive+document over renaming to decisions_surfaced: in-repo only context-tools.ts references the field, but external consumers/plugin sessions key on the current name. Whether to ALSO add a semantic admission floor for decisions (parity with the entry-side D12 floor) is a small design call worth taking at the same time.
RISK: Renaming breaks out-of-repo consumers; documenting is risk-free. Adding a decisions floor would change briefing composition and needs the same calibration caution the report gives for hardcoded floors.
--- S1C-fabricated-scope [OPEN] effort:M
CLAIM: assemble on a nonexistent scope returns a full imperative briefing citing real files instead of scope_resolved:false; ask is to validate the scope.
EVIDENCE: grep of src/ — 'scope_resolved' and 'scope_match' appear nowhere in the codebase; no scope validation exists on any tool | src/engine/context-assembler.ts:129-167 — for any scope string, store-wide unfloored semantic decision admission still populates the briefing from task-text similarity alone | src/engine/context-assembler.ts:741-804 — formatForLLM then renders DECISIONS TO RESPECT, FILES TO CHECK BEFORE WRITING (real affected_files), and the imperative YOUR NEXT STEP (line 887) with no scope-existence caveat
FIX: In assemble, compute scope_resolved = scopeDecisions.length > 0 || scopeEntries.length > 0 || graph query matched || path exists under projectRoot (assembler lacks projectRoot today — plumb it via ctor/config); special-case 'project'. When false: set scope_resolved:false in the tool metadata and have formatForLLM lead with 'Scope <s> matched no decisions, entries, or files — likely a typo' while demoting/labeling semantically-sourced content instead of rendering the imperative blocks.
RISK: False negatives on intentionally virtual scopes (planned-but-uncreated dirs, tag-like scopes, worktree path differences) — must be a labeled notice, not a hard error, or Gate 1 starts failing legitimate first-touch sessions.
--- S2B-token-estimate [OPEN] effort:M
CLAIM: token_estimate echoes the requested budget rather than measuring the serialized response on both assemble and why; why's more[] sits outside the budget pinned at 50 and a ~9K-char floor exists below which max_tokens does nothing.
EVIDENCE: src/engine/context-assembler.ts:318-353,531 — assemble token_estimate = tokensUsed = sum of estimateTokens(raw item text) consumed during SELECTION, not the serialized response; saturated fills necessarily land at ~budget | src/engine/context-assembler.ts:739-777 — the rendered briefing tiers decisions to top-3 full + 2 summaries + '+N more', so at large budgets hundreds of selected-and-costed decisions are never rendered — the code mechanism behind the measured 5.7x over-report; handoffs (578-598) and related_entities are rendered but never costed | src/engine/decisions.ts:729-733,765 — why token_estimate = tier-1 tokensUsed only; more[] entries cost nothing against the budget
FIX: Compute token_estimate at the tool layer from the actual serialized payload (estimateTokens(briefing + JSON.stringify(metadata)) in context-tools.ts; estimateTokens(JSON.stringify(result)) in decision-tools.ts). Make max_tokens bound the whole payload: in why, charge more[] entries against the budget and shrink the 50-cap when the budget is small; in assemble, cost decisions by their RENDERED form (tier-aware: full for slots 1-3, 120-char summary for 4-5, ~0 beyond) so selection and rendering agree.
RISK: User-visible numbers change (agents/detectors calibrated to the pegged-estimate signature, including this report's own published detector, would need the docs note); shrinking more[] under low budgets removes one-liners agents may rely on; tier-aware costing couples selection to presentation and needs tests to avoid under/over-fill oscillation.
--- S4-1-double-render [OPEN] effort:S
CLAIM: Every truncated-summary warning renders twice in assemble (truncated preview then 'Full summary: <full text>'), and read's detail begins with 'Full summary: ' in 40/50 entries — one upstream defect; ask is to render once.
EVIDENCE: src/tools/record-tools.ts:19-33,357-362 — summaries over 200 chars are truncated to 197+'…' and the FULL text is prepended to detail as 'Full summary: <full>' (deliberate lossless-write decision, comment cites 2026-07 field findings); second write path at :486-492 | src/engine/pending-processor.ts:78 — third write path: detail = `Full summary: ${summary}\n${detail}` | src/engine/context-assembler.ts:707-709 — assemble renders `- **${w.summary}**` followed by detail on the next line, so the truncated preview is a strict prefix of the 'Full summary:' line — the exact doubling measured; no dedupe exists at HEAD
FIX: Render-time dedupe, keeping the on-disk lossless format: in formatForLLM (context-assembler.ts:708) and in read's response shaping (blackboard-tools), when detail starts with 'Full summary: ' AND the entry summary minus its trailing '…' is a prefix of that full text, emit the full text once (as the bolded line) and strip the duplicate. Guard on the prefix-match so caller-authored details that legitimately begin 'Full summary:' with different content pass through untouched.
RISK: Very low — payload strictly shrinks; only hazard is the prefix-match guard being too loose and eating a legitimate detail, hence match against the summary, not just the literal marker.
NOTES: All seven items verified against HEAD (main @ 0a0aa89, 2.15.0) by reading src/engine/context-assembler.ts, src/tools/context-tools.ts, src/engine/decisions.ts, src/tools/record-tools.ts, src/engine/pending-processor.ts, src/embeddings/search.ts, plus git log -S and CHANGELOG dating. Net: 6 OPEN, 1 PARTIAL, 0 FIXED — the 2.7.0-2.15.0 waves fixed adjacent read-path defects but none of this cluster; nothing here is tracked in docs/field-responses/ or TWINING-DESIGN-SPEC.md (this 08-15 report is first contact). Asks map: 7→ASM-disclosure, 9→ASM-false-empty+S1C, 11→ASM-budget-order, 12→S2B, 19→docs-only companion noted inside ASM-budget-order's fix_sketch (effort S, ships the caller-side workaround today). Two accuracy corrections to the report worth carrying into any response memo: (1) the fill order is not literally warnings→needs→continue-work→decisions — warnings fill first from the full budget, non-warnings then compete score-ranked under a 90% cap, needs get a safety pass, and handoffs bypass the budget entirely — but the starvation conclusion is confirmed; (2) the semantic-admission floor added in 2.9.0/2.10.0 (SEMANTIC_ADMISSION_FLOOR) gates blackboard entries only — decisions still enter unfloored store-wide, which is load-bearing for both ASM-population and S1C. Suggested landing order: ASM-disclosure plumbing first (S, unlocks ASM-false-empty), then S4-1 and the ask-19 docs line (both S), then budget floor + token_estimate (M, shared test surface), then scope validation (M, needs projectRoot plumbing). CHANGELOG.md has no 2.13.0-2.15.0 entries (tags exist; headings stop at 2.12.0) — out of cluster but noticed while dating fixes. Read-only rules honored: no writes, no builds/tests, no server against the live store (twining gates skipped for that reason).
CHALLENGE ASM-disclosure -> REVISED corrected:OPEN NOTE: All triage code citations verified accurate and the fix_sketch/effort S/risk remain valid (add: guard the context-assembler.ts:780 emptiness sentence on the in-scope population per report fix (c)). But the verdict is OPEN, not PARTIAL: zero of the report's ask shipped at HEAD. The credited 'fixed half' fails both prongs — warnings_omitted predates the report (2.6.0 = the audit build, and it never reached the metadata JSON), while archived/superseded excluded counts address a different disclosure axis (status exclusion, D3/D10) and cannot express '0 of 2,021 active shown'. PARTIAL would let the remediation plan treat part of an entirely-unshipped ask as done.

===== CLUSTER: scope-semantics =====
--- SCOPE-segment-boundary [OPEN] effort:M
CLAIM: Scope matching is raw-string bidirectional prefix (D.startsWith(Q) || Q.startsWith(D)) with no path-segment awareness, so 'spec' returns more than 'specs/', a single char selects half a store, and fabricated deep paths under a real dir return hundreds; ask is segment-boundary matching that preserves ancestor inheritance.
EVIDENCE: src/utils/scope.ts:7-9 — scopeMatches(a,b) is exactly `a.startsWith(b) || b.startsWith(a)`, no segment logic | src/storage/decision-store.ts:77-82 — getByScope filters index via scopeMatches on scope and affected_files (symbols are exact), so 'spec' string-prefixes every 'specs/…' record | src/storage/sqlite/sqlite-stores.ts:216-217 — sqlite backend identical
FIX: Rewrite scopeMatches in src/utils/scope.ts to segment-boundary semantics: a matches b iff a === b, or a.startsWith(b.endsWith('/') ? b : b + '/'), or the reverse (treat trailing-slash and slashless dir forms as equivalent). All ~10 call sites (decision-store, sqlite-stores, blackboard-store, triage, handoff-store, exporter, verify, coordination, context-assembler inline copies at lines 178/905/999/1013) go through or should be converted to the shared helper — also replace the inline bidirectional startsWith copies in context-assembler.ts with scopeMatches so the fix lands everywhere. Preserve exact-match for non-path scopes (symbols, 'project').
RISK: Read-path behavior change across every tool at once. Field stores with sloppy scope strings (bare words like 'auth', compound scopes like 'specs/ + rfcs/', 'spec.md §2.7') will stop matching path queries they loosely matched before — intended for the bug, but could hide decisions agents relied on finding; needs a test sweep over existing scope-matching tests and ideally a field-store dry-run comparison.
--- SCOPE-matched-via [OPEN] effort:M
CLAIM: Nothing distinguishes exact-match rows from ancestor-inherited ones; ask is a per-row matched_via field (exact|descendant|ancestor|affected_files) plus directly_scoped_count on why.
EVIDENCE: src/engine/decisions.ts:56-73 — WhyDecision has no matched_via; WhyResult (84-117) has no directly_scoped_count | src/engine/decisions.ts:149-164 — whySpecificity already computes exactly the needed tiers internally (3 = exact scope/file/symbol, 2 = descendant or affected-file prefix, 1 = ancestor) but the value is used only for ranking and discarded | grep matched_via/directly_scoped across src/ and docs/ — zero hits
FIX: In src/engine/decisions.ts, have whySpecificity (or a sibling) return a label alongside the rank ({via: 'exact'|'descendant'|'ancestor'|'affected_files', rank}); stamp matched_via on each WhyDecision/WhyCompactDecision in why() (decisions.ts:716-744), and add directly_scoped_count (count of exact + descendant scope-owned matches, excluding affected_files/ancestor) to WhyResult. Cheap because the classification logic already exists.
RISK: Low — additive fields. Main risk is semantic: deciding whether affected_files-exact counts as 'directly scoped' (recommend no, keep it a separate via label) and keeping the label consistent with whatever segment-boundary fix lands, since the two asks interact.
--- SCOPE-tool-divergence [OPEN] effort:S
CLAIM: assemble, why, and summarize return three different decision populations for one scope and none documents its semantics; ask is published per-tool scope semantics plus a scope_match field.
EVIDENCE: src/storage/decision-store.ts:77-82 — why's population = scope bidirectional OR affected_files bidirectional OR symbol exact (confirms the report's 'relevance population' hypothesis it could not confirm from outside) | src/engine/context-assembler.ts:903-907 — summarize filters index by scope-string only, no affected_files, so why >= summarize always: the report's 4/4 prediction is code fact at HEAD | src/engine/context-assembler.ts:112-115 and 303-346 — assemble uses getByScope filtered to active/provisional, unions semantic matches, then budget-fills; context-tools.ts:50 reports decisions_count as the budget-truncated selection length, explaining the '1 → 320 budget-dependent' row
FIX: Doc pass: state each tool's population in its registerTool description (context-tools.ts, decision-tools.ts) and in TWINING-REFERENCE.md — why counts scope+affected_files+symbols; summarize counts scope-string ownership; assemble reports a budget-truncated briefing selection, not a population count. Additive scope_match/count_semantics string field echoed in each response. Optionally align summarize to getByScope or explicitly brand it 'ownership counts'.
RISK: Doc-only version is near-zero risk. Aligning summarize's population to why's would change field-visible counts and should be a deliberate, changelogged choice, not a drive-by.
--- SCOPE-dot-slash [OPEN] effort:S
CLAIM: './specs/' silently resolves to a different population than 'specs/', and scope '' is accepted by assemble but INVALID_INPUT on why; ask is normalize or reject consistently.
EVIDENCE: grep for './' normalization across src/ — none exists; scopeMatches compares raw strings, and './specs/' fails startsWith in both directions against 'specs/…' scopes, so the populations differ exactly as reported | src/tools/decision-tools.ts:162-167 — why rejects empty/absent scope with INVALID_INPUT ('Provide a scope or a list of decision ids') | src/tools/context-tools.ts:22-43 and context-assembler.ts:97-115 — assemble has no emptiness check; scope '' flows into getByScope('') where every string startsWith('') matches the whole store
FIX: Add a normalizeScope helper in src/utils/scope.ts (strip leading './', collapse '//', trim) applied at every tool boundary (assemble, why, summarize, triage, post, decide/record) AND at write time so stored scopes converge; pick one empty-scope policy — recommend assemble rejecting '' like why does, since triage.ts:94 already documents '' as a filter that silently matches everything — and apply it uniformly.
RISK: Rejecting '' in assemble breaks any field caller currently passing it (they get an error instead of project-wide output — arguably the honest change but a behavior break worth a changelog line); read-side-only normalization would leave already-stored './'-prefixed scopes unmatched, so write-side normalization or a housekeeping repair is needed with it.
--- SCOPE-affected-files-readback [OPEN] effort:S
CLAIM: No read-only tool exposes affected_files on decisions — why omits it in both the scoped rows and the ids drill-down — even though the divergence discipline keys on it; ask is to expose it on why ids-detail at least.
EVIDENCE: src/engine/decisions.ts:807-824 — whyByIds drill-down returns context, alternatives, scope, domain, constraints, depends_on but NOT affected_files/affected_symbols; scoped rows (717-728) omit it too | grep affected_files across src/tools/*.ts — every hit is input-side (record-tools, amend, housekeeping candidate reporting); no read tool returns it | src/engine/exporter.ts — filters by affected_files (55-61) but never prints them in the export
FIX: One-field fix: add affected_files (and affected_symbols) to the WhyDecision push in whyByIds at src/engine/decisions.ts:807-824 — the drill-down path is explicitly documented as carrying 'the detail the tiered response withholds', so this is the designed slot. Optionally add affected_files_count to scoped full-tier rows to keep budget impact nil.
RISK: Trivial — ids mode is unbudgeted by design so token growth is bounded by the caller's id list; decisions with huge file lists could bloat a drill-down (cap or count-plus-truncate if worried).
--- S3F-cost-scoping [OPEN] effort:S
CLAIM: Bidirectional prefix means a narrow scope also matches all ancestor-scoped items, so scoping down does not cut cost, contradicting the 'use narrowest scope' guidance; ask is per-tool documentation or a scope_match parameter.
EVIDENCE: src/engine/triage.ts:251,276 — triage still filters via bidirectional scopeMatches, so triage({scope:'specs/registry-service/'}) still returns 'specs/'-scoped items | src/tools/triage-tools.ts:28 — schema still documents only 'bidirectional prefix match' with no descendants-only option; no scope_match parameter exists anywhere (grep: zero hits) | src/instructions.ts:20 — MCP instructions still say 'use the narrowest scope that fits', the exact guidance the report says cost semantics contradict
FIX: Two-lane: (a) doc — add one sentence to instructions.ts:20 and the why/assemble/triage tool descriptions stating that narrow scopes inherit all ancestor-scoped items, so narrowing bounds relevance, not cost; (b) optional parameter — scope_match: 'descendants' | 'bidirectional' (default bidirectional for compatibility) threaded to scopeMatches call sites in triage/why, letting cost-sensitive callers opt out of ancestor inheritance.
RISK: Doc lane is zero-risk. Parameter lane must default to bidirectional or every existing gate/caller's population silently shrinks; also interacts with the segment-boundary fix (SCOPE-segment-boundary) — land that first so 'descendants' has sane semantics.
NOTES: All six cluster items are OPEN at HEAD (2.15.0): none of the 2.7.0-2.15.0 scope work (scopeless-handoff D12 fix, entity-scope accumulation, staleness per-segment probing) touched the read-path matcher, and no ask is tracked in docs/field-responses/ or TWINING-DESIGN-SPEC.md as a named backlog item. Notable for the response memo: (1) the report's explicitly-unconfirmed S1-B hypothesis (why = scope OR affected_files relevance population; summarize = scope-string ownership) is confirmed by code at HEAD — decision-store.ts:77-82 vs context-assembler.ts:903-907 — which also validates their 'no read tool exposes affected_files' meta-finding; (2) item 2 is cheaper than the report assumes because whySpecificity (decisions.ts:149-164) already computes the exact/descendant/ancestor classification internally; (3) triage.ts:92-95 already contains a comment naming the empty-scope hazard that assemble still has, i.e. the codebase fixed this class in one tool and not the others. Recommended landing order: segment-boundary matcher first (shared helper, converts context-assembler's four inline bidirectional copies to scopeMatches as part of it), then matched_via/directly_scoped_count, then normalization + per-tool docs — the later fixes' semantics depend on the matcher's.

===== CLUSTER: lifecycle-render =====
--- S1D-frozen-status [PARTIAL] effort:M
CLAIM: assemble's continue-work items carry a status snapshot frozen at handoff-write time, never re-joined to the decision store at render, so a [BLOCKED] label can contradict twining_why's live status:active for the same ULID; ask 10: join to live status at render or drop lifecycle words.
EVIDENCE: src/engine/context-assembler.ts:729-734 — render reads r.status directly from the stored handoff record ('[x]' / blockedIcon / '[ ]'); no lookup against the decision store | src/utils/types.ts:429-434 — HandoffResult is {description, status, artifacts?, notes?}: no decision_id field exists to join on; the ULID in the field's example lived in free-text description | src/engine/context-assembler.ts:717-727 + 731 — D12 age stamps: items >=1 day old render '(Nd ago)' and '[BLOCKED Nd]' (commit 017a878, first released in v2.10.0), so the field's 18-day fossil would now self-describe as '(acknowledged) (108d ago)' with '[BLOCKED 108d]' items
FIX: Fixed half: age stamping + scope-bleed fix (v2.10.0, D12) make stale replays self-describing. Open half: no live-status join and lifecycle words still render. Fix in context-assembler.ts render (lines 729-734): cheapest, per the report's own alternative, stop emitting lifecycle vocabulary for aged items — render blocked items older than N days as plain '[ ]' with the age stamp, reserving '[BLOCKED]' for same-day handoffs. Fuller fix: add optional decision_ids?: string[] to HandoffResult (types.ts:429) plus a best-effort ULID regex scan of description, resolve each against decisionStore at render, and annotate '[BLOCKED — decision now active/superseded]' or drop the item when every referenced decision is closed.
RISK: Live-join adds decision-store reads to the assemble hot path and ULID parsing can false-positive on quoted IDs; dropping lifecycle words for aged items loses signal on genuinely long-blocked work. Handoff surface is deprecated (#33), so heavy investment here may be building on a tier scheduled for v3 removal.
--- S1D-ack-clearing [OPEN] effort:S
CLAIM: twining_acknowledge does not clear per-item results[i].status, and acknowledged handoffs keep replaying in assemble's continue-work section.
EVIDENCE: src/storage/handoff-store.ts:150-152 — acknowledge sets only acknowledged_by/acknowledged_at on the record; results[] untouched | src/storage/sqlite/sqlite-stores.ts:691-704 — sqlite twin identical: stamps ack fields and index acknowledged=true, never touches results[i].status | src/engine/context-assembler.ts:580 — assemble calls handoffStore.list({scope, limit: 5}) with no acknowledged filter; acknowledged handoffs are returned and rendered
FIX: In context-assembler.ts, either filter acknowledged entries out of the continue-work listing (add acknowledged filter at the list call, line 580, or skip at render, line 715), or collapse an acknowledged handoff to a single line ('acknowledged Nd ago — checklist suppressed; twining_status for detail') with no per-item [BLOCKED] rows. Prefer suppression over mutating results[i].status in acknowledge — rewriting caller-authored result records destroys the historical record and the file/sqlite/export mirrors would all need the same rewrite. Note #33: since the tool pair is deprecated, suppressing acknowledged replay is also a cheap step toward the v3 removal path.
RISK: Teams using acknowledge as 'received' rather than 'done' lose continuation context for acknowledged-but-incomplete work; gate suppression on result_status==='completed' or age >= N days to keep fresh partial handoffs visible. Touches assemble output shape — snapshot tests on the briefing text will churn.
--- S1F-root-freeze [OPEN] effort:S
CLAIM: Project root is resolved once at server startup (--project > TWINING_PROJECT > cwd) with no per-call override, cd cannot retarget a running server, and misrouted calls are near-silent because only twining_status returns project; ask: echo the resolved store root in every tool response.
EVIDENCE: src/index.ts:41-52 — resolveProjectRoot(process.argv, env, cwd) called once at boot; result threaded into createServer, never re-resolved | src/utils/project-root.ts:66-92 — precedence exactly as the report states (--project > TWINING_PROJECT > cwd, plus the worktree redirect on the cwd branch only) | src/utils/errors.ts:7-11 — toolResult wrapper is a bare JSON.stringify(data); no envelope field identifies the store
FIX: Implement the report's cheaper alternative: echo the resolved root in every response. Mechanically, give toolResult (src/utils/errors.ts:7) a module-level setStoreIdentity(projectRoot) called from createServer (src/server.ts:64), and have it append one short key (e.g. project: basename, or store_root: absolute path) to every success payload — one edit point covers all ~20 tools since they all return through toolResult. Apply the same to toolError so misrouted failures are also self-announcing. Do NOT take the per-call project/scope_root parameter route without a design pass — it reopens store lifecycle, locking, and dashboard assumptions (one server = one store) and the report itself frames it as the expensive option. The startup freeze itself is inherent to stdio MCP spawning and is working as designed; the defect is the silence.
RISK: A few tokens of overhead on every response in token-sensitive field repos (keep the key short, basename not absolute path — though basename alone cannot distinguish two repos with the same directory name); response-shape assertions in the test suite and any strict field-side parsers will need updating; toolError enrichment must not mask the original error code.
NOTES: Audit machine ran 2.6.0/2.13.0; HEAD is 2.15.0. The D12 fixes (age stamps + scopeless-handoff scope-bleed, commit 017a878, first tag v2.10.0) are why the second reviewer's assemble probes showed no continue-work item — the fossil's empty-scope match was the surfacing mechanism, consistent with the report's own split verdict on the S1-D replay. The (ii) half of S1-D (vacuous CLAUDE.md emptiness proof in the field repo's own docs) is a field-repo documentation issue, not a twining-mcp code defect, and was not part of my assigned items. The twining_handoff/twining_acknowledge pair is deprecated (#33, CHANGELOG 384/440, removal decision deferred to v3) — relevant sizing context for both S1-D fixes but it tracks the tools' existence, not the assemble replay behavior, so neither item is ALREADY_TRACKED by it.
CHALLENGE S1D-frozen-status -> CONFIRMED NOTE: PARTIAL stands with the split as drawn. Two non-verdict-affecting corrections to the evidence prose: (1) the '(108d ago)/[BLOCKED 108d]' render is speculative — if the fossil record is scopeless, the HEAD scope-bleed fix means it would not surface in scoped assembles at all, which is an even stronger form of the fixed half; (2) '18-day fossil' conflates the decision's closure (18 days before audit) with the handoff record's age (~108 days if written near store creation 2026-04-30). Worth adding to the open-half description: same-day blocked items still render bare '[BLOCKED]' with no age stamp, so the contradiction is not merely 'self-describing but stale' — it is fully unmitigated inside the first 24 hours.

===== CLUSTER: payload-contract =====
--- PAY-export [OPEN] effort:M
CLAIM: twining_export has no limit/max_tokens/section selector, and its bidirectional prefix scope filter made the narrowest legal call (one file) return 236K tokens — more than its whole directory.
EVIDENCE: src/tools/export-tools.ts:19-26 — inputSchema contains only optional `scope`; no limit, max_tokens, or section selector of any kind | src/engine/exporter.ts:52 — blackboard filter is `e.scope.startsWith(scope) || scope.startsWith(e.scope)` (bidirectional prefix, unchanged) | src/engine/exporter.ts:58-61 — decisions match scope OR affected_files bidirectionally, so a single-file scope pulls in every ancestor-scoped decision with full rationale
FIX: In src/tools/export-tools.ts add `sections` (enum array: decisions|blackboard|graph), `limit` (per-section cap), and `max_tokens`; thread them into Exporter.exportMarkdown (src/engine/exporter.ts) which already builds sections separately. Default the decisions section to summary rows (id, summary, status, scope) with `full_rationale: true` opt-in — mirroring what_changed's shape per the report's own template. Keep bidirectional matching (it is shared design) but let the budget knobs bound the output.
RISK: Handoff consumers expect a complete snapshot; changing the default to summary-shape silently breaks 'export then re-import context' workflows — safest is full-by-default plus knobs, or a loud truncation banner in the markdown when a budget binds.
--- PAY-read [OPEN] effort:M
CLAIM: twining_read returned 166K tokens at defaults; detail is 96% of bytes; no token_estimate and no max_tokens exist.
EVIDENCE: src/tools/blackboard-tools.ts:94-118 — inputSchema has entry_types/tags/scope/since/limit only; no max_tokens, no detail control | src/engine/blackboard.ts:208-222 — read() returns `{ entries, total_count }` with entries carried wholesale (full `detail` field), default limit 50 | src/engine/triage.ts:141-151 — the detail_preview/detail_truncated collapse helper already exists in-repo but is used only by triage, not read
FIX: Truncate `detail` per entry by default (reuse triage's collapse helper at src/engine/triage.ts:141, with a larger cap, emitting per-entry detail_truncated), add an `ids:` full-fetch parameter copying twining_why's escape hatch (src/tools/decision-tools.ts:152-157), and add response fields returned/truncated/omitted_count (derivable from the existing total_count vs entries.length) plus a token_estimate over the serialised payload.
RISK: Any agent that reads entry.detail for full content breaks on the truncation default — needs a CHANGELOG-flagged behavior change and the ids: full-fetch path landing in the same release; tests asserting full detail in read output will need updating.
--- PAY-recent [OPEN] effort:S
CLAIM: twining_recent cost 47K tokens at its own default n=20 with no disclosure and no budget knob.
EVIDENCE: src/tools/blackboard-tools.ts:191-202 — schema is n (default 20) + entry_types only; no max_tokens, no disclosure | src/engine/blackboard.ts:248-254 — recent() returns bare `{ entries }` with full detail, no total, no counts | src/tools/blackboard-tools.ts:217-226 — the merged decision rows are already summary-shaped (id/timestamp/summary/domain/scope/status/confidence), so only the blackboard half carries the weight
FIX: Apply the same per-entry detail truncation as the PAY-read fix (shared helper), and add returned/token_estimate fields; recent needs no total (it is a head-of-stream view) but should say `detail_truncated` per entry and disclose n_applied.
RISK: Same detail-consumer break as PAY-read, smaller blast radius; ship the two together so the surface behaves uniformly.
--- PAY-neighbors [ALREADY_TRACKED] effort:S
CLAIM: twining_neighbors has no limit parameter in its schema, depth up to 3, and ~50% of the payload is verbatim duplication (entity.properties.summary == relation.properties.decision_summary).
EVIDENCE: src/tools/graph-tools.ts:114-126 — inputSchema is entity/depth/relation_types only; no limit exists | src/engine/graph.ts:127-148 — BFS pushes every `{ entity, relation, direction }` unbounded; only depth is clamped (line 81, max 3) | src/engine/graph-auto-populator.ts:45 vs :59,:74 (also :166,:180) — decision entity gets properties.summary = input.summary and every decided_by relation gets properties.decision_summary = input.summary, so the duplication is structural and still produced at HEAD
FIX: Add `limit` to the schema (src/tools/graph-tools.ts:114) and cap the BFS result push (src/engine/graph.ts:137) with truncated/total disclosure; in the response, drop relation.properties.decision_summary when the decision entity is present in the same payload (or return a reduced relation projection {type, source, target, direction}) — halves the payload per the report's measurement.
RISK: Dashboard graph views and any consumer reading decision_summary off the relation would lose it — check dashboard JS before dropping the field; a projection-only change in the tool layer (leaving stored properties intact) is the safe cut.
TRACKED: D13 ask-2 'neighbors work in its accepted reduced form' — open follow-up in docs/field-responses/2026-08-13-wave2-ship-memo.md:185; note the tracked scope names edge-completeness, hop depth, and lifecycle awareness but NOT a `limit` schema param or the summary-string dedup — the backlog item should be widened to absorb both when picked up
--- PAY-graph-query [OPEN] effort:S
CLAIM: twining_graph_query returns 10 of 4,504 entities with no total, count, or truncation flag at any limit.
EVIDENCE: src/engine/graph.ts:179 — `return { entities: matches.slice(0, maxResults) }` — matches.length is computed and then discarded | src/tools/graph-tools.ts:166-173 — tool layer passes the engine result straight through, adding nothing | src/engine/decisions.ts:1269-1332 — search_decisions got exactly this fix (total_matched + returned) in 2.9.0, proving the pattern and leaving graph_query as the straggler
FIX: In GraphEngine.query return `{ entities: sliced, total_matched: matches.length, returned: sliced.length, truncated: matches.length > sliced.length }`; update the QueryResult type in the same file and the handful of tests asserting the shape. Purely additive.
RISK: Negligible — additive fields; only exact-shape test assertions and any dashboard consumer doing strict key checks could notice.
--- S3D-contract [PARTIAL fixed_in: partially: 2.7.0 (assemble excluded-counts), 2.9.0 (search_decisions/query total_matched)] effort:L
CLAIM: Truncation disclosure is inconsistent across the surface (triage/why good; read partial; recent/query/graph_query/neighbors/what_changed silent; assemble/verify worst) — the ask is one uniform response contract (total, returned, truncated, omitted_count) on every record-returning tool.
EVIDENCE: FIXED half — src/engine/decisions.ts:1269-1332: search_decisions now returns honest total_matched + returned (shipped 2.9.0, CHANGELOG.md:93-97), retiring the report's 'broken' row (measured on their 2.6.0 build) | FIXED half — src/engine/blackboard.ts:228-232: twining_query returns total_matched (pre-slice, noise-floored) + fallback_mode, so 'query: silent' no longer holds | FIXED half — src/tools/context-tools.ts:47-61: assemble returns decisions_count/warnings_count/needs_count/archived_excluded_count/superseded_excluded_count/token_estimate (D3, 2.7.0 per CHANGELOG.md:217) — no longer literally 'none'
FIX: Define one shared disclosure shape (total, returned, truncated, omitted_count — optionally token_estimate) in src/utils/types.ts and thread it through read, recent, graph_query, neighbors, what_changed, verify, and assemble (assemble additionally needs total_in_scope alongside its included-counts, i.e. porting why's fields per the report's own template). Mechanical per-tool work once the type exists; the search tools already demonstrate the pattern.
RISK: Wide but shallow blast radius: every tool's response shape changes additively, so exact-shape tests, dashboard fetchers, and the bundled plugin server all need the same release; keep fields additive-only to avoid breaking field consumers mid-upgrade.
--- S3D-prose-vs-fields [OPEN] effort:S
CLAIM: why's human-readable message mentions the 50 one-liners but never the omitted 228 while its JSON fields are exact — the report's most repeated pattern is prose contradicting correct fields, and it asks for an audit of every prose string against the fields beside it.
EVIDENCE: src/tools/decision-tools.ts:176 — message is `${result.more?.length ?? 0} more decision(s) in scope returned as one-liners in 'more' — call twining_why with ids: [...]` — omitted_count and total_in_scope never appear in the sentence, byte-for-byte the audited behavior | src/engine/decisions.ts:757-759 — the JSON beside it is correct: omitted_count, truncated, total_in_scope: matches.length all returned | src/engine/decisions.ts:733,742 — entries past the WHY_MAX_COMPACT one-liner cap increment omitted_count silently, confirming the 76-vs-304 misread mechanism the report describes
FIX: Rewrite decision-tools.ts:176 to state the whole population: e.g. `N decisions in scope: X full, Y one-liners in 'more', Z omitted — call twining_why with ids: [...] or raise max_tokens`. Then do the report's asked-for one-pass audit of every prose string in src/tools/ and src/engine/context-assembler.ts formatForLLM against the sibling fields (the assemble emptiness sentence and commits empty-object cases live in other clusters but fall out of the same pass).
RISK: Minimal — string change plus tests asserting the exact message; the audit pass is read-mostly. The one design nit: keep the sentence generated FROM the fields (single source) so it cannot drift again.
NOTES: All classifications are against HEAD = 0a0aa89 (2.15.0). Cluster-level picture: the payload/budget surface asks (report asks 13-14, lines 861-862) are genuinely new wave-3 material — nothing in the 2.8.0-2.15.0 fix train touched export/read/recent/neighbors/graph_query payload shape (git log confirms export-tools.ts and graph-tools.ts untouched since tool-surface registration), and the ship memo's open-follow-ups list contains no payload item except the neighbors reduced-form entry. The disclosure-contract item is the only PARTIAL: the audit machine's 2.6.0 build predates the 2.7.0 assemble excluded-counts and the 2.9.0 total_matched fixes, so three rows of the report's S3-D table are stale, but the report's structural point (no uniform contract; 5+ tools fully silent) still holds at HEAD. Ask 14's read-half (detail restates summary in 40/50 entries) is folded into PAY-read per the report's own accounting ('cosmetic there — the unbounded detail field is the real defect'); its assemble double-render half is S4-1, outside this cluster. One cross-cluster observation worth carrying: the report's proposed template shapes already exist in-repo (triage's detail_preview/detail_truncated helper, why's ids: escape hatch, search_decisions' total_matched/returned pair, what_changed's summary-only rows), so every fix in this cluster is propagation of proven in-repo patterns, not new design — matching the report's own claim at line 486.
CHALLENGE S3D-contract -> REVISED corrected:PARTIAL NOTE: The PARTIAL verdict and nearly all of the split are correct, but the FIXED half over-credits twining_query and the fix_sketch consequently drops it from the remediation list. At HEAD, query is no longer silent (total_matched is a genuine pre-slice, noise-floored count — src/embeddings/search.ts:92-121 semantic path, :125-147 keyword path, threaded through src/engine/blackboard.ts:225-245 and spread into the tool response at src/tools/blackboard-tools.ts:175), but it still fails the item's actual ask: (a) it has no `returned` field (unlike search_decisions), and (b) its merged `decisions` sub-array maps only decisionSearch.results and discards decisionSearch.total_matched (src/tools/blackboard-tools.ts:163-172), so the decision tier of a query response is silently truncated at limit with no disclosure. twining_query must be added to the OPEN-half tool list and to the fix_sketch thread-through set (read, recent, query, graph_query, neighbors, what_changed, verify, assemble). Everything else stands as cited: effort L, risk statement, and 'not already tracked' are all confirmed.

===== CLUSTER: verify-tool =====
--- VER-hoist [OPEN] effort:S
CLAIM: checkDrift calls decisionStore.getByScope(scope) inside the per-file loop on the stale branch, giving O(stale_files x scope population); report asks for a one-line hoist.
EVIDENCE: src/engine/verify.ts:369 — `const allDecisions = await this.decisionStore.getByScope(scope);` sits inside `for (const file of decision.affected_files)` (line 356), inside the stale branch (line 367) | src/engine/verify.ts:329-399 — `scope` is a fixed parameter of checkDrift, so the call is loop-invariant exactly as the report says | git log -S 'getByScope' -- src/engine/verify.ts — last touches are the original P0/P2 verify commits (b2d9d70, 4390d9e); no fix shipped in 2.7.0-2.15.0
FIX: Hoist `const allDecisions = await this.decisionStore.getByScope(scope)` to the top of checkDrift (before line 350's decision loop), or lazily memoize it on first stale hit. Note verify() line 81 already fetched the same scope population — could pass `allDecisions` down and avoid the query entirely.
RISK: Essentially none: no writes occur during iteration, so the hoisted snapshot is semantically identical; only a drift-supersession unit test asserting call counts could need updating.
--- VER-git-spawn [OPEN] effort:M
CLAIM: Drift spawns one `git log` subprocess per affected_file (~45ms each; minutes at 5,465 entries); report asks for batching or caching.
EVIDENCE: src/engine/verify.ts:357 — `this.execGit(["log", "--format=%aI %H", "-1", "--", file])` inside the per-file loop, one execFileSync spawn per entry | src/engine/verify.ts:356 — no dedup: the same file appearing under multiple decisions is re-queried each time | CHANGELOG.md grep for verify/drift — no perf change since the tool shipped (entries at lines 646-741 are v1.x-era behavior changes only)
FIX: Minimal (S): memoize per unique file in a Map<file, {date,hash}> for the run — cuts spawns to distinct files. Full (M): one `git log --format=%aI %H --name-only` history walk (or chunked `git log -1 --` with many pathspecs) building the file map in a single subprocess, falling back to per-file on parse failure.
RISK: Single-walk batching must match per-file `git log -1 -- <path>` semantics for renames and files absent from HEAD history; the memoize-only variant is behavior-identical and safe.
--- VER-skip-as-pass [OPEN] effort:S
CLAIM: When `git rev-parse` fails, drift returns {status:'skip', decisions_checked:0} in ~40ms, indistinguishable at a glance from a clean pass; report asks for a loud distinct status naming the unmet precondition.
EVIDENCE: src/engine/verify.ts:342-344 — `if (gitCheck === null) return { status: "skip", decisions_checked: 0, stale: [] };` with no reason field | src/utils/types.ts:573-584 — DriftCheck has only status/decisions_checked/stale; no field can carry the precondition | src/engine/verify.ts:107-113 — summary renders as bare `drift: skip`; src/engine/verify.ts:496 — checkConstraints has the same reasonless skip
FIX: Add `skip_reason` to DriftCheck (and ConstraintsCheck), e.g. `"git unavailable in <projectRoot> (rev-parse failed)"`; render it in the summary (`drift: skip (git unavailable)`); optionally treat skip as fail when the caller passed the check in `fail_on` — note `fail_on` is currently accepted at verify-tools.ts:40 but never read by the engine, an adjacent dead parameter worth wiring in the same pass.
RISK: Additive fields are safe; tests or downstream parsers asserting the exact `summary` string or check object shape need touching.
--- VER-clean-set [OPEN] effort:S
CLAIM: ~65% of checked decisions pass silently and are never listed; there is no way to ask which decisions passed and on what basis.
EVIDENCE: src/utils/types.ts:573-584 — DriftCheck output is only {status, decisions_checked, stale}; nothing represents the clean set | src/engine/verify.ts:394-398 — return path lists only stale entries; clean decisions are neither counted separately nor named | src/engine/verify.ts:350-353 — decisions with empty affected_files are silently excluded from decisions_checked too, so even the checked/clean denominator basis is unstated
FIX: Add `clean_count` (= decisions_checked minus distinct stale decision ids), `skipped_no_affected_files` count, and a `basis` string ("no affected_file has a commit newer than the decision timestamp") to DriftCheck; optionally `clean_decision_ids` behind an opt-in flag to avoid payload growth. This is report ask 15 (Tier 2).
RISK: Count-only default keeps payload flat; listing ids by default would worsen VER-unbounded, so gate the list behind a flag.
--- VER-unbounded [OPEN] effort:M
CLAIM: verify returns 110-198K-token payloads with three uncapped lists, no max_items/since watermark/partial results, and no published cost; calls exceeded the 120s MCP timeout.
EVIDENCE: src/tools/verify-tools.ts:18-46 — full input schema is scope/checks/agent_id/fail_on; no max_items, since, or suppress-detail flag exists | src/engine/verify.ts:271 (ignored_details), :306 (blind_decisions), :381 (stale push) — all three lists are built uncapped with no truncation or omitted_count | src/tools/verify-tools.ts:16-17 — tool description says nothing about expected cost or payload size; no partial-result/checks_completed mechanism in VerifyResult (src/utils/types.ts:601-612)
FIX: Add `max_items` (per-list cap with `omitted_count`), a `since` watermark on drift (only decisions newer than the timestamp), and `detail: "summary"|"full"` to suppress the three lists; add `checks_completed` for partial results if a time budget is hit; state expected cost in the tool description. The report files this under Tier-3 (ask 18) but the caps/flags half is mechanical; only the partial-results protocol needs a small design pass.
RISK: Downstream consumers (e.g. agents repairing every stale entry) silently lose rows if caps default too low — default caps generous, always emit omitted_count, keep uncapped available via explicit opt-in.
--- VER-temporal-only [DESIGN_DECISION_OPEN] effort:L
CLAIM: Drift's predicate is purely commit-date > decision-timestamp with no content signal, so populated-but-untouched affected_files are undetectable by construction, and Gate-2 (record before commit) makes every record predate its own files so staleness accrues by construction; report asks for a content hash at decision time and itself files this as Tier-3 architectural.
EVIDENCE: src/engine/verify.ts:367 — sole predicate is `new Date(fileDate) > new Date(decision.timestamp)`; the only escape is the domain/scope supersession probe at :369-378, also temporal | src/utils/types.ts:576-583 — stale entry carries exactly the six fields the report enumerated; no content/hash/digest/diff field exists anywhere in DriftCheck or the decision schema (grep 'hash' in src/engine/decisions.ts finds only commit_hashes links and embedContentHash, an embedding-cache key, not file content) | Gate-2 mechanics confirmed structurally: decisions are recorded pre-commit (CLAUDE.md Gate 2), so the commit touching affected_files always post-dates decision.timestamp and fires the predicate — no negative-delta path exists in the code
FIX: Owner design call (report Tier-3 ask 18, second half): capture a per-affected-file content signal at record time — HEAD blob SHA via one `git ls-tree` per record, stored alongside affected_files — and have drift compare current blob SHA, flagging only real content change. A cheap partial mitigation needing no schema change: decisions already carry `commit_hashes` (linkCommit); exempt a file whose last modifying commit is in the decision's own commit_hashes, which directly cancels the Gate-2 self-staleness term.
RISK: Write-path schema change touching both backends, the mirror/exporting store, and migrate; retroactive decisions have no hash so drift must run a mixed-mode predicate; blob-hash capture at record time races the user's uncommitted edits (worktree hash vs HEAD hash is itself a design choice).
NOTES: All six claims verified against HEAD src/engine/verify.ts (untouched by the 2.7.0-2.15.0 fix waves — git log shows no perf/bounding commits since the tool's original P0/P2 implementation). Report line refs (verify.js:236/259 in their 2.6.0 bundle) map to src/engine/verify.ts:342-344 and :369 at HEAD; the code is unchanged, so the old-build caveat does not rescue any item in this cluster. Nothing here is tracked in docs/field-responses/ or TWINING-DESIGN-SPEC.md. Adjacent finding recorded under VER-skip-as-pass: the `fail_on` input parameter (verify-tools.ts:40) is accepted and passed to the engine but never read — dead surface worth wiring or removing when touching skip semantics. Suggested fix ordering: hoist + per-file memoize (S, kills both perf terms) -> skip_reason + clean_count (S) -> caps/flags (M) -> content-signal design decision (L).

===== CLUSTER: search-counts =====
--- CNT-total-matched [FIXED fixed_in: 2.9.0 (2026-08-13), commit 017a878 'wave A field-defect fixes — D9 honest search counts' (not 2.13.0 as the item text guessed; 2.13.0 merely was the first bundle the audit saw containing it)] effort:S
CLAIM: At 2.6.0 total_matched equaled the post-slice page length; verify that at HEAD it is a pre-slice count after filters with a returned field on both keyword and semantic paths.
EVIDENCE: /Users/dave/code/twining-mcp/src/engine/decisions.ts:1262-1272 — response docblock: 'Pre-slice match count — never capped by limit (field D9)' plus 'returned: Page size actually delivered' | /Users/dave/code/twining-mcp/src/engine/decisions.ts:1283-1299 — domain/status/confidence filters applied to the index BEFORE search, so the count is over the filtered set | /Users/dave/code/twining-mcp/src/engine/decisions.ts:1331-1332 — semantic delegate path forwards engine total_matched and sets returned = results.length
FIX: Nothing needed for twining_search_decisions. Optional tidy-up: add returned to twining_query's blackboard lane and forward decisions_total_matched/decisions_returned in the twining_query merge (src/tools/blackboard-tools.ts:169-175).
RISK: n/a — already shipped; only the twining_query asymmetry remains if anyone cares
--- CNT-schema-version [OPEN] effort:S
CLAIM: Docs in the field store are pinned to different generations of total_matched semantics and the response carries no version signal; report asks for a count_semantics or schema_version field in search responses.
EVIDENCE: grep for count_semantics/schema_version across /Users/dave/code/twining-mcp/src returns zero hits in any tool response; CHANGELOG.md has no such entry | /Users/dave/code/twining-mcp/docs/field-responses/2026-08-16-memo-send.md — the pending field memo covers upgrade steps and D14/D15 verification questions but does not track a response-version-signal ask | /Users/dave/code/twining-mcp/docs/field-responses/2026-08-13-wave2-ship-memo.md:155 — 'total_matched changed meaning (deliberately — that was D9)' — the project itself documents a semantics generation change with no in-band way for a caller to detect which generation it is on
FIX: Add a constant field to twining_search_decisions (and twining_query) responses, e.g. count_semantics: "pre_page_floored_v2" — set where the engine result is shaped (src/engine/decisions.ts:1319-1334/1382-1400 or in the tool handler at src/tools/decision-tools.ts:407); document the literal's meaning in the tool description and CHANGELOG. Bump the literal on any future semantics change.
RISK: Strictly additive response key, so main risks are response-shape snapshot tests and the tool-description token-budget CI gate (unaffected — this is a response field, not description text). Choose a stable literal that only changes when count semantics change, or it becomes noise.
--- CNT-status-filter [FIXED fixed_in: 2.9.0, commit 017a878 — limit-clamping is gone; filters run before search and the count is pre-slice] effort:S
CLAIM: At the audited build, total_matched under a status filter was limit-clamped (min(limit, population)); the report also asserts the field-documented failure state 'total_matched 0 with rows present' cannot occur.
EVIDENCE: /Users/dave/code/twining-mcp/src/engine/decisions.ts:1283-1299 — status filter narrows the index first; :1331/1397 — count is pre-slice over that filtered set, never bounded by limit | /Users/dave/code/twining-mcp/src/embeddings/search.ts:185-207 — semantic path pushes EVERY row into the ranked page but increments matched only for raw cosine >= 0.3, so at HEAD 'total_matched: 0 with rows present' CAN occur and means 'nothing above the noise floor' — the report's 'that state cannot occur' was true only of their 2.6.0 session | /Users/dave/code/twining-mcp/src/tools/decision-tools.ts:364 — this is documented on the wire: 'returned is the delivered page size, which may include sub-floor rows for ranking context'
FIX: No code change. In the field-response memo, correct the record: 2.6.0 = limit-clamped (their measurement), 2.9.0+ = floored pre-slice count where 0-with-rows is meaningful; point them at the tool-description sentence that states it.
RISK: Only risk is messaging: the reply memo must tell the field their retired instrument was retired for the RIGHT behavior observed on the NEW build, and un-retire nothing
--- CNT-triage-by-kind [PARTIAL] effort:S
CLAIM: triage counts.open.total conflates blackboard posts with provisional decisions (150x over-report as a ratify queue) while counts.open.by_kind.decision is exact but undocumented; ask is to document by_kind and consider renaming/splitting total.
EVIDENCE: /Users/dave/code/twining-mcp/src/engine/triage.ts:265-266 — provisional decisions enter openItems as kind 'decision'; :314-323 — counts.open.total = all open items (posts + provisionals) while by_kind.decision = countKind(openItems,'decision'), exact | /Users/dave/code/twining-mcp/docs/TRIAGE-SPEC.md:357-366 (§3.5-3.6) — for_agent excludes only blackboard-sourced kinds, so by_kind.decision is unaffected by the exclusion filter; spec documents the fully-enumerated by_kind guarantee | /Users/dave/code/twining-mcp/src/utils/types.ts:752-766 — TriageResult type documents by_kind with pre-truncation guarantee
FIX: One sentence in the twining_triage description (src/tools/triage-tools.ts:19-21): 'counts.open.by_kind splits the lane by kind; by_kind.decision is the exact provisional-decision (ratify-queue) count and is unaffected by for_agent; do not read counts.open.total as a ratify queue.' Leave total as-is.
RISK: Tool-description edits count against the token-budget CI gate (red since 2.6.0, pre-existing). Renaming/splitting total would break the documented pages-sum-to-total contract (triage.ts:339-341) — do NOT rename; document instead.
TRACKED: Shape (not the agent-facing doc) is specced in docs/TRIAGE-SPEC.md §4 — 'by_kind keys always fully enumerated', lines 446-460, with by_kind.decision called the 'ratify-lane' basis at line ~460
--- CNT-canonical-derivation [PARTIAL] effort:S
CLAIM: Report recommends twining_status.provisional_decisions as the canonical ratify-queue count — the only form that cannot be made wrong by a bad query; check the field exists at HEAD and whether anything declares it canonical.
EVIDENCE: /Users/dave/code/twining-mcp/src/tools/lifecycle-tools.ts:51-53,171 — provisional_decisions = index.filter(status==='provisional').length, a direct query-proof index count, returned in every twining_status response | /Users/dave/code/twining-mcp/src/tools/lifecycle-tools.ts:33-34 — the tool description says only 'decision counts' generically; nothing names provisional_decisions or the ratify-queue role | /Users/dave/code/twining-mcp/TWINING-DESIGN-SPEC.md:545,691 — provisional_decisions appears in the documented status/why response shapes, but no 'canonical derivation' language anywhere (grep 'ratify' over the spec: zero hits)
FIX: Amend the twining_status description (src/tools/lifecycle-tools.ts:33-34): 'provisional_decisions is the canonical ratify-queue count — a direct index count that no query can distort; for a scoped breakdown use twining_triage counts.open.by_kind.decision.' Mirror one line in TWINING-REFERENCE.md and in the next field memo.
RISK: Description token budget only. Also keep the two instruments consistent in the docs: status.provisional_decisions is store-wide; triage counts.open.by_kind.decision is the scoped variant — declare both roles or agents will 'discover' a discrepancy under a scope filter.
NOTES: Cluster theme: every code-level defect here was fixed in 2.9.0 (commit 017a878, 2026-08-13, D9); everything still open is a one-sentence documentation/declaration gap plus one additive response field. Key inversion for the reply memo: the audit ran a 2.6.0 session, so (a) their limit-clamping measurement was correct for their build but is dead at HEAD, and (b) the relevance-floor mechanism they 'could not reproduce' and used to discredit the field's provisional decision IS the actual HEAD (>=2.9.0) behavior — the field's provisional was right for the build it was written against. The S3-A 'regression window, 1.30.0->1.31.0 diff' theory is therefore wrong: nothing regressed; two builds (npm-cached 2.6.0 vs bundle 2.13.0) served concurrent sessions — that is the distribution cluster's S0-B territory (as is server-version-in-responses; MCP handshake carries version at src/server.ts:236 but tool responses do not). The strongest single follow-through for this cluster is CNT-schema-version: it is the report's self-described cheapest permanent fix, it is validated by the report's own four-agent investigation, and it is tracked nowhere in this repo today.
CHALLENGE CNT-total-matched -> CONFIRMED NOTE: FIXED verdict stands with fixed_in 2.9.0 / commit 017a878 exactly as claimed.
CHALLENGE CNT-status-filter -> CONFIRMED NOTE: FIXED stands. The claim's key inversion is verified in source: at HEAD 'total_matched: 0 with rows present' is real, meaningful behavior (noise floor), so the report's 'cannot occur' was true only of its 2.6.0 session — and the report itself (line 407) marked the floor mechanism 'do not record as refuted', so no retracted claim is being re-litigated.
CHALLENGE CNT-triage-by-kind -> REVISED corrected:PARTIAL NOTE: Verdict PARTIAL and the fixed/open split are correct; one already_tracked_as citation detail is wrong: TRIAGE-SPEC.md:458-460 attaches the 'ratify-lane' label to counts.open.irreversible (the truncation-proof tally of provisional irreversible decisions), NOT to by_kind.decision — the spec never calls by_kind.decision the ratify-lane basis (nearest is §2 line 188 naming twining_promote the ratify act for kind=decision). The tracking reference should read: shape specced at TRIAGE-SPEC.md §4 lines 444-452 ('by_kind keys always fully enumerated'); the ratify-lane language at line 460 concerns irreversible, a different counter.
CHALLENGE CNT-canonical-derivation -> CONFIRMED NOTE: PARTIAL stands as declared (mechanism exists and was never broken; canonical declaration absent everywhere). Trivial path nit in fix_sketch: the reference doc is docs/TWINING-REFERENCE.md, not repo-root TWINING-REFERENCE.md.

===== CLUSTER: search-quality =====
--- QUAL-relevance-floor [PARTIAL fixed_in: 2.9.0 (017a878) for the floored count half] effort:S
CLAIM: No relevance threshold — nonsense queries return confident rows; ask is an above_threshold count against a configurable relevance_floor plus a top_relevance echo, explicitly not a hardcoded constant.
EVIDENCE: src/embeddings/search.ts:41 — export const SEARCH_NOISE_FLOOR = 0.3, hardcoded; comment: 'measured ~0.26-0.28 cosine for nonsense queries on MiniLM' | src/embeddings/search.ts:98,189 — semantic-mode total_matched counts raw cosine >= floor, so total_matched IS the asked-for above-threshold count (results page itself is not floored, per lines 26-30) | src/tools/decision-tools.ts:364 — tool description documents the ~0.3 noise floor and 'absence is not expressible with a ranker; use twining_why + total_in_scope'
FIX: Add search.relevance_floor to DEFAULT_CONFIG (default 0.3 for continuity), plumb from config into SearchEngine (constructor param) and context-assembler's SEMANTIC_ADMISSION_FLOOR; echo top_relevance (results[0]?.relevance ?? null) as a top-level field in DecisionEngine.searchDecisions and BlackboardEngine.query responses. Do not change the default value.
RISK: Raising the floor changes total_matched semantics for existing consumers and the tool-description text pinned to '~0.3'; keep default 0.3 and update the description to say 'configurable'. A per-store misconfigured floor (e.g. 0.9) would make total_matched read 0 everywhere — clamp or document.
--- QUAL-superseded-projection [OPEN] effort:S
CLAIM: superseded_by is not projected onto search_decisions rows (status is), so a superseded row cannot point at its replacement without a second why() call.
EVIDENCE: src/engine/decisions.ts:1250-1260 — search result row type: id, summary, domain, scope, confidence, status, timestamp, relevance, commit_hashes — no superseded_by | src/engine/decisions.ts:1320-1330 and 1383-1393 — both row-mapping sites (semantic and keyword fallback) omit the field | src/engine/decisions.ts:695,727 — why() already projects superseded_by conditionally, so the projection pattern exists in the same file
FIX: In both mapping sites in DecisionEngine.searchDecisions add ...(r.decision.superseded_by ? { superseded_by: r.decision.superseded_by } : {}) (mirror the why() spread at decisions.ts:695); update the row type and tool description.
RISK: Minimal — purely additive optional field; only risk is consumers doing exact-shape assertions in tests (repo's own vitest snapshots may need the field added).
--- QUAL-exclude-status [OPEN] effort:S
CLAIM: search_decisions status filter is single-valued, so active-or-provisional / exclude-superseded is inexpressible; ask is array-valued status or an exclude_status parameter.
EVIDENCE: src/tools/decision-tools.ts:377-380 — status: z.enum([...]).optional(), single value only | src/engine/decisions.ts:1243-1247,1290-1294 — engine filter type is status?: DecisionStatus with an equality filter over the index | Contrast: why() has include_superseded (decisions.ts:95-101 region) — the lifecycle guard exists on why but not on search, exactly as the report says (report line 708)
FIX: Widen the zod schema to z.union([statusEnum, z.array(statusEnum)]) (or add exclude_status: z.array(statusEnum)); in DecisionEngine.searchDecisions normalize to a Set and filter with set membership at decisions.ts:1290. Keep single-string accepted for back-compat.
RISK: Schema back-compat: MCP clients caching the old schema still send strings — union type handles it. Filtering happens pre-search over the index in memory, so no store/index changes and no ranking interaction (the deboost path is untouched).
--- QUAL-lifecycle-rank [FIXED fixed_in: 2.9.0 (commit 017a878, 2026-08-13)] effort:S
CLAIM: Superseded records outrank their own superseders (both retired originals 6 positions above replacements); D9 was supposed to ship lifecycle-aware ranking.
EVIDENCE: src/embeddings/search.ts:47-48 — RETIRED_STATUS_DEBOOST = 0.75 over {superseded, overridden, archived}, with comment citing field D9 ('a superseded original must not outrank its own amendment') | src/embeddings/search.ts:169-172,190,203 — ordering-only deboost in the semantic path, guarded against negative cosines, never affecting total_matched | src/engine/decisions.ts:1374-1375 — same 0.75 weight applied in the keyword-fallback path via WHY_RETIRED_STATUSES (defined 138-142: superseded/overridden/archived)
FIX: None needed — shipped in 2.9.0.
RISK: None — already shipped.
--- QUAL-blackboard-blind [OPEN] effort:M
CLAIM: search_decisions silently excludes the blackboard tier (0.729 exact-match post invisible while a 0.58 decision is returned) with no blackboard_matches_excluded flag; also verify twining_query's decisions tier is literally the same call.
EVIDENCE: src/engine/decisions.ts:1249-1272 — searchDecisions response shape is results/total_matched/returned/fallback_mode only; no blackboard count or flag anywhere in the path | src/tools/decision-tools.ts:364 — description redirects existence tests to twining_why but never states the blackboard tier is out of scope — the silent boundary the report measured is intact | src/tools/blackboard-tools.ts:163-168 — twining_query's decisions tier calls options.decisionEngine.searchDecisions(args.query, undefined, args.limit ?? 10): the identical engine method with no filters, confirming the report's byte-identical-ranking measurement structurally (no ranker to drift), and per-tier limits (line 167 vs engine.query at blackboard.ts:239)
FIX: In DecisionEngine.searchDecisions, when a searchEngine exists, compute a count-only blackboard pass — add a countMatches(query) helper on SearchEngine that reuses the already-embedded query vector against the 'blackboard' index — and return blackboard_matches_excluded: <count above floor>. One sentence in the tool description naming the tier boundary.
RISK: Adds a second index load + full-corpus dot-product pass to every search_decisions call (latency on large stores); embed the query once and share the vector. Also decide whether the count respects entry-type semantics (legacy 'decision' mirror entries would inflate it — see QUAL-mirror-dedup-trap).
--- QUAL-query-gaps [PARTIAL fixed_in: 2.9.0 (017a878) for the blackboard-tier total_matched] effort:M
CLAIM: twining_query has no status/domain/confidence filters, no total_matched at all, default limit 10 vs search's 20, and entry_types silently empties the decision tier; report recommends aiming the query-store gate at twining_query while retaining search_decisions.
EVIDENCE: src/engine/blackboard.ts:225-245 — query() returns total_matched since 2.9.0 (comment 'field D9'), so the report's 'complete key set: decisions, fallback_mode, results' is stale at HEAD: total_matched is present for the posts tier | src/tools/blackboard-tools.ts:169-175 — the decisions tier maps decisionSearch.results only, discarding its total_matched/returned — the decision tier still has no count inside twining_query | src/tools/blackboard-tools.ts:139-151,166 — input schema is query/entry_types/limit only; searchDecisions is called with undefined filters — no status/domain/confidence passthrough, so S2-D/S3-A population derivations remain inexpressible: OPEN
FIX: In the twining_query handler: (1) surface decisions_total_matched/decisions_returned from the discarded decisionSearch fields (2-line change); (2) when entry_types excludes 'decision', emit decisions_excluded_by_filter: true instead of a bare []; (3) add optional status/domain/confidence passthrough to the decisions tier (engine already accepts filters); (4) align defaults deliberately or document the 10/20 difference in both tool descriptions. Pointing the query-store gate at twining_query is field-side governance, not a repo change.
RISK: Changing the default limit alters payload size/shape for every existing field agent mid-flight — the report itself shows default-shape changes are the likeliest silent breakage; prefer documenting over changing. New fields are additive and safe.
--- QUAL-mirror-dedup-trap [PARTIAL fixed_in: 2.0.0-beta.2 (f65102f, issue #30) for the write path — predates the auditor's 2.6.0 build] effort:M
CLAIM: Blackboard mirrors of decisions carry different ULIDs and different relevance than their store twins (tier ID intersection 0), so dedup-by-id double-counts and a mirror can outrank its original; ask is a stable cross-ref on the mirror.
EVIDENCE: src/engine/decisions.ts:448-451 — 'No blackboard cross-post (issue #30): decisions live only in the decision store'; mirror-posting is NOT how decisions reach the blackboard at HEAD, and wasn't at their 2.6.0 either | git show f65102f^:src/engine/decisions.ts (removed code, old lines 251-263) — the mirror posted entry_type 'decision' with summary slice + rationale and NO decision-id reference, so the 105 legacy mirrors in their store cannot be cross-referenced by id retroactively | src/engine/archiver.ts:61,70 — keep_decisions defaults true: legacy 'decision' entries are exempt from archive sweeps, so they persist indefinitely on pre-v2-origin stores
FIX: Don't delete (mirrors may be the only surviving trace on stores hit by the S0 rehydration defect). Cheapest honest fix: annotate — in twining_query's posts-tier mapping, stamp legacy_mirror: true on entries with entry_type 'decision' (only the retired writer produced that type), and add a housekeeping preview count of legacy mirrors so field stores can see their exposure. Optionally a migrate/housekeeping step that content-matches mirrors to store twins (summary-slice equality) and back-fills a source_decision_id where the match is unique.
RISK: Content-matching is fuzzy — a wrong source_decision_id back-fill is worse than none (only accept exact 200-char summary-slice matches with a unique candidate). Archiving/removing mirrors risks destroying evidence on stores where the decision store lost rows. Annotation-only is safe.
NOTES: Audit build context confirmed: the auditor's 2.6.0 predates 2.9.0's D9 fixes (SEARCH_NOISE_FLOOR total_matched counting, 0.75 retired-status deboost, blackboard-tier total_matched on query — all commit 017a878) but postdates the 2.0.0-beta.2 mirror-cross-post removal, so their 105 measured mirrors are legacy store data, not live writes. None of the surviving asks (configurable relevance_floor, top_relevance echo, superseded_by row projection, array/exclude status, blackboard_matches_excluded, query-tier counts/filters, mirror cross-ref) appear as named backlog items in docs/field-responses/ or TWINING-DESIGN-SPEC.md — the OPEN/PARTIAL residues are genuinely untracked. Notable interaction for planning: HEAD's hardcoded 0.30 floor is exactly the value the report's own data withdrew as unsafe (plausible-absent class peaks at 0.337), which strengthens the report's do-not-hardcode ask; and DecisionEngine already holds blackboardEngine, so the blackboard_matches_excluded fix needs no wiring. Report asks 3-5 (lines 848-850) map onto QUAL-relevance-floor, QUAL-blackboard-blind, and QUAL-superseded-projection/QUAL-exclude-status respectively — fixing items 2+3 together touches the same two row-mapping sites in decisions.ts and could ship as one small change.
CHALLENGE QUAL-relevance-floor -> CONFIRMED NOTE: Split is drawn correctly. One emphasis worth carrying into the plan: the shipped half (total_matched floored at 0.3) is itself only weakly responsive — the report's plausible-absent class peaks at 0.337, above HEAD's floor, so the count still reads nonzero for exactly the absence class that matters. The triage's own evidence and risk note already state this; the open half (configurable relevance_floor + top_relevance echo) is what closes it.
CHALLENGE QUAL-lifecycle-rank -> REVISED corrected:PARTIAL NOTE: The ranking-inversion half is genuinely FIXED in 2.9.0 (017a878) — deboost verified in both semantic and keyword paths, and the report's measured instance re-scores resolved (0.6815*0.75=0.511 and 0.6675*0.75=0.501 both fall below the active superseders at 0.5841/0.5807). But this item is the report's S3-C, whose 'Fix, cheapest first' list ranks the rank penalty THIRD; HEAD shipped only that third-ranked remedy. Open at HEAD and tracked nowhere: (1) superseded_by is not projected onto search rows — the searchDecisions row mapping (decisions.ts ~1319-1329) emits id/summary/domain/scope/confidence/status/timestamp/relevance/commit_hashes with no superseded_by, while why() does project it (decisions.ts:695,727,816); (2) search cannot express 'exclude superseded' — status filter is a single-valued enum (decision-tools.ts:377-380), include_superseded exists only on why (decision-tools.ts:142), and grep finds no exclude_status/array-valued status anywhere in src/ or docs/. A bare FIXED verdict drops the report's two preferred, cheaper remedies (also its top-level recommendation #5, line 850) from the remediation plan. Fix sketch for the open half: project superseded_by into the search row mapping (pure projection, effort S, risk: +1 field payload growth) and accept a status array or exclude_status param in twining_search_decisions (effort S, risk: schema change must stay backward-compatible with single-string callers).
CHALLENGE QUAL-query-gaps -> CONFIRMED NOTE: Split verified leg-by-leg; the fixed/open boundary is drawn in the right place. Also confirmed the triage's premise that the engine already accepts filters (searchDecisions(query, filters, limit) signature), so the passthrough item in the fix sketch is a handler-only change as stated.
CHALLENGE QUAL-mirror-dedup-trap -> CONFIRMED NOTE: Verdict and split stand. One detail in the fix sketch needs correction before implementation: 'only the retired writer produced that type' is not strictly true — the direct-post rejection of entry_type 'decision' only dates to 20e6aa6 (2026-02-17), so stores with pre-Feb-2026 history can also hold direct agent-posted 'decision' entries that never had a store twin. A legacy_mirror stamp keyed on entry_type alone therefore means 'legacy-era decision entry', not 'auto-mirror'; the sketch's exact-200-char-summary-match + unique-candidate safeguard for source_decision_id back-fill already handles the distinction, and no HEAD production path can create new entries of that type (engine.post rejects it at blackboard.ts:110-113 unless _internal, whose only callers are tests).

===== CLUSTER: hygiene =====
--- HYG-record-fail-rate [PARTIAL fixed_in: 1.24.0 (over-length summary truncated instead of rejected — was ~38% of field calls); 1.24.0/2.7.0 (per-decision and per-finding failures became soft response fields, not call failures)] effort:S
CLAIM: twining_record fails 10.3% of calls with INVALID_INPUT (106s worst case); reduce the rate or return a repairable error.
EVIDENCE: CHANGELOG.md:483 — '`twining_record` no longer rejects over-length summaries… failed ~38% of field calls with INVALID_INPUT' (1.24.0), so the dominant historical cause predates their 2.6.0 build | src/tools/record-tools.ts:26-34 — truncateSummary caps at 200 chars, preserving full text in detail; status post can no longer trip the blackboard length check | src/tools/record-tools.ts:440-473 + 487-511 — decide() and finding-post failures are caught per item into decision_errors/finding_errors on a SUCCESS response; each message names the field ('rationale is required' etc.), so surviving errors are repairable
FIX: Make reason_rejected optional in the alternatives schema of twining_record (src/tools/record-tools.ts:228) and twining_decide (decision-tools.ts:51) to match engine semantics; reject empty summary at the zod layer with a message ('summary must be non-empty — one sentence of what you did') instead of letting it reach the engine; pair with HYG-metrics-fields so residual INVALID_INPUT causes become measurable per error message. The 106s worst case is almost certainly first-call embedder model load, not validation — out of scope here.
RISK: Loosening reason_rejected is purely permissive (engine already accepts absence). No caller can depend on the current rejection. Rate itself cannot be re-measured from this repo — it is field data from 2.6.0-and-earlier builds.
--- HYG-metrics-fields [OPEN] effort:S
CLAIM: metrics.jsonl lacks response_bytes, result_count, and scope, so context cost is unmeasurable across the install base.
EVIDENCE: src/utils/types.ts:624-631 — MetricEntry is exactly {tool_name, timestamp, duration_ms, success, error_code?, agent_id}; no size, count, or scope field | src/analytics/instrumented-server.ts:55-62 — the wrapper has the full serialized result in hand (content[0].text, lines 33-37) at the moment it writes the metric, so response_bytes is one Buffer.byteLength away | docs/field-responses/2026-08-13-wave2-ship-memo.md:177-192 — open-follow-ups list does not include metrics fields; not tracked anywhere in docs/
FIX: In instrumented-server.ts success path: response_bytes = Buffer.byteLength(first.text ?? ''), scope = typeof args.scope === 'string' ? args.scope : undefined; result_count = length of the first top-level array in the parsed response JSON (generic heuristic; per-tool accuracy optional later). Add the three optional fields to MetricEntry (types.ts:624). Error path records response_bytes of the error payload.
RISK: metrics-store.ts aggregations and the dashboard must tolerate additive fields (they parse JSONL per line — additive is safe). Generic result_count can be wrong for nested-shape tools; document it as best-effort.
--- HYG-agent-id [OPEN] effort:M
CLAIM: 84.6% of telemetry rows have agent_id 'unknown', and the field is free-text abused with whole prompts; constrain or derive it.
EVIDENCE: src/analytics/instrumented-server.ts:89-98 — extractAgentId returns args.agent_id verbatim if present, else 'unknown'; no derivation from session or environment | src/tools/coordination-tools.ts:88-90 — twining_register's agent_id is bare z.string(): no max length, no charset constraint, so an entire task prompt is a valid id (same in all ~13 tool schemas, e.g. context-tools.ts:30-33, record-tools.ts:340-343) | grep for TWINING_AGENT/AGENT_ID env across src/ and plugin/ — no env-derived identity exists
FIX: Two-part: (1) normalize, don't reject — in extractAgentId and at each engine boundary, truncate agent_id to ~64 chars and collapse whitespace (a prompt-sized id becomes its first line), optionally warn in the response; (2) derive a stable per-process default (e.g. 'main-<session ULID prefix>' minted once at server start in index.ts) used when agent_id is absent, so telemetry rows and verify's assembly check join on something real instead of 'unknown'. Add a format hint to every agent_id .describe().
RISK: Changing the absent-case default from 'main'/'unknown' alters verify's per-agent assembly join and registry touch behavior — keep 'main' as the logical identity and only suffix in telemetry, or verify's history goes discontinuous. Hard rejection (zod regex) would break existing field callers; normalization avoids that.
--- HYG-dead-artifacts [OPEN] effort:M
CLAIM: v1 migration leftovers (embeddings/*.index, blackboard.jsonl, config.yml.pre-migrate.bak) stay in place after migration and actively mislead investigations; delete or move under archive/.
EVIDENCE: src/migrate/forward.ts:11-12 — 'Legacy files are never modified or deleted (they are their own backup)': the no-cleanup behavior is a deliberate migration invariant, and nothing later relocates or marks them | src/migrate/forward.ts:117-121 — migration notes mention embeddings are rebuilt in sqlite but write no marker into the store saying the on-disk .index files are dead | src/migrate/config-edit.ts:43 — .pre-migrate.bak is created and never cleaned; src/migrate/auto.ts has no cleanup/relocate step (grep)
FIX: Respect the 'legacy files are their own backup' invariant with an additive fix: on finalize (forward.ts after setStorageBackend, ~line 244) write .twining/MIGRATED-README.md stating 'backend=sqlite since <date>; blackboard.jsonl, decisions/, embeddings/*.index, config.yml.pre-migrate.bak are frozen v1 artifacts, not live state', and/or add a housekeeping flag (archive_legacy_v1) that moves them under .twining/archive/v1-legacy/ with execute:true. Only for sqlite-backend stores — embeddings/*.index are LIVE on the files backend.
RISK: The legacy tier is the only non-db copy if records/ ingest ever regresses (the S0-B hasSqliteState defect class rehydrates from records/, not legacy) and stale 1.x clients still read it under the mixed-team lockout — so moving is safer than deleting, and the README-marker alone removes the misleading-evidence problem at zero risk.
--- HYG-wal [OPEN] effort:S
CLAIM: 9.77MB uncheckpointed WAL against a 65.7MB database; no checkpoint policy exists.
EVIDENCE: src/storage/sqlite/db.ts:114-115 — openDatabase sets journal_mode=WAL and busy_timeout only; grep across src/ finds zero wal_checkpoint calls | src/index.ts:76-78 — the only shutdown hook is telemetry.shutdown(); the db handle is never closed on exit, so the shutdown checkpoint sqlite performs on close never runs | src/engine/housekeeping.ts — no PRAGMA/vacuum/checkpoint maintenance pass (grep)
FIX: (1) In housekeeping's execute path add db.exec("PRAGMA wal_checkpoint(TRUNCATE);") reported as {wal_checkpointed_bytes}; (2) add SIGINT/SIGTERM handlers in index.ts that close the database (close() checkpoints); optionally PRAGMA wal_autocheckpoint tuning at open. Requires housekeeping to reach the raw db handle — thread it from backend-factory alongside the stores.
RISK: TRUNCATE checkpoints block while any reader holds a snapshot — bounded by busy_timeout 5000 and non-fatal on failure. Shutdown handler must not close mid-write; do it after the transport drains. WAL size itself is a performance/ops nuisance, not a correctness bug.
--- HYG-discover-min-score [OPEN] effort:S
CLAIM: twining_discover's min_score defaults to 0 so 95 zero-capability-overlap agents return as matches (~3,250 tokens of zero information).
EVIDENCE: src/tools/coordination-tools.ts:148-151 — min_score: optional, described as 'default: 0' | src/engine/coordination.ts:164-167 — filter applies only `if (input.min_score !== undefined)`; omitted means no filter at all | src/engine/coordination.ts:70-72 — total_score = capability_overlap*0.7 + liveness_score*0.3, so a zero-overlap agent still scores up to 0.3 (0.03 even when 'gone') and passes any threshold ≤ that
FIX: In CoordinationEngine.discover: when required_capabilities is non-empty, default-exclude rows with capability_overlap === 0 (equivalently default min_score to 0.31, just above the max liveness-only score), return excluded_zero_overlap: <n> alongside total_registered so absence stays reportable, and pass min_score: 0 explicitly to restore old behavior. Update the schema description in coordination-tools.ts:151.
RISK: Behavior change for any caller using discover as a roster listing — twining_agents remains the roster tool, but release-note it. In stores where no agent has capabilities recorded (the report's exact situation) discover now returns an empty page; the excluded count is what makes that honest rather than confusing.
--- HYG-archive-detectability [PARTIAL] effort:M
CLAIM: Auto-archive threshold (500) counts something other than the 3,330 live mirror files; committed archive rolls are undetectable except via git log --diff-filter=D; archive/ is read by nothing (retrieval black hole).
EVIDENCE: src/engine/blackboard.ts:181-200 — the trigger counts to_archive.length from partitionArchivable (exempt classes + retain_recent excluded), not total live entries; TWINING-DESIGN-SPEC.md:652-653 documents this exact semantics ('the auto-archive threshold counts only archivable entries') as the #35 loop fix — so sub-claim 1 is documented design, not a defect (though the config key name max_blackboard_entries_before_archive still misleads) | src/engine/archiver.ts:209-219 — every roll posts a live 'Archive: N entries archived' finding (summarize defaults true, line 164), which is mirrored and committed — so a committed roll IS detectable from the board itself, softening sub-claim 2 | src/engine/tombstones.ts:25-28 + housekeeping-tools.ts:146 — archived blackboard entries are DELETED from the live board with their only copy appended to gitignored .twining/archive/*.jsonl, and twining_unarchive does NOT restore them
FIX: For the open half: (a) cheap visibility — add archive stats to twining_status/housekeeping preview (archive file count, total archived entries, last roll date) by statting archive/*.jsonl, making rolls visible without git archaeology; (b) retrieval — an opt-in include_archived flag on blackboard search/read that streams archive/*.jsonl with an archived: true marker on each row. Also rename-or-document the config key's partition semantics in its schema description.
RISK: Archive files can contain pre-1.24.0 loop junk and unparseable lines (compact_archives exists precisely because of that) — any reader must skip bad lines and stay off hot paths. The threshold semantics change is deliberately load-bearing against the #35 feedback loop; do not 'fix' the count itself.
--- HYG-commits-found-flag [OPEN] effort:S
CLAIM: twining_commits returns byte-identical {decisions:[]} for an unknown/typo'd SHA and a real-but-unlinked SHA; add found:bool or error on unknown hash.
EVIDENCE: src/tools/decision-tools.ts:343-346 — handler passes commit_hash straight to engine.getByCommitHash and returns the result; no existence probe, no found flag | src/engine/decisions.ts:881-904 — getByCommitHash returns only {decisions: [...]}; an unmatched hash (typo or unlinked) yields the same empty array | src/tools/decision-tools.ts:11-16 — registerDecisionTools receives twiningDir but not projectRoot, so the fix needs the repo root threaded in (path.dirname(twiningDir) or a new param)
FIX: In the twining_commits handler, when decisions.length === 0 run execFileSync('git', ['cat-file','-e', `${sha}^{commit}`], {cwd: projectRoot, timeout: 3000}) and return commit_exists: true|false|"unknown" (unknown for non-git dirs/ambiguous short SHAs/git errors) plus a message: 'commit exists but has no linked decisions' vs 'no such commit in this repository — check the hash'. Purely additive; no error-on-unknown needed since the field disambiguates.
RISK: Subprocess adds ~10ms and a git dependency on a previously store-only tool — degrade to 'unknown' on any failure. Short-SHA ambiguity: cat-file -e errors on ambiguous prefixes; report that as its own message rather than false.
--- HYG-archived-visibility [FIXED fixed_in: 2.9.0 (commit 017a878, wave A / D9-D10)] effort:S
CLAIM: Whether archive_stale's output (status:'archived' decisions) is visible to search was untestable in the field store; does HEAD define and test it?
EVIDENCE: src/engine/decisions.ts:1282-1299 — searchDecisions filters the index only by caller-supplied domain/status/confidence: archived decisions are INCLUDED in the candidate set by default | src/engine/decisions.ts:138-143 + 1370-1373 — WHY_RETIRED_STATUSES = {superseded, overridden, archived} gets a 0.75 ordering-only de-boost in keyword mode; src/embeddings/search.ts:48 — same RETIRED_STATUSES set in semantic mode, so both paths agree | src/tools/decision-tools.ts:366 — tool description states it: 'Retired decisions (superseded/overridden/archived) are included but de-ranked — check status and follow superseded_by'
RISK: One residual wart worth a line in any reply memo: searchDecisions with a status filter that matches zero candidates short-circuits with fallback_mode: true (decisions.ts:1301-1303), which mislabels 'no candidates' as 'keyword fallback' — this is the report's incidental observation that the status:'archived' query was their only fallback_mode:true call.
NOTES: Method: pure reads + git log against HEAD (main, 2.15.0); no tests run, no files written, per hard rules. Context that bounds several verdicts: the audit ran 2.6.0/2.13.0 builds, and their metrics.jsonl aggregates all store history including pre-1.24.0 sessions, so the 10.3% record-failure rate is not reproducible from HEAD code — the dominant cause (200-char rejection) died in 1.24.0. None of the seven OPEN/PARTIAL items appear in the tracked open-follow-ups list (docs/field-responses/2026-08-13-wave2-ship-memo.md:177-192) or elsewhere in docs/ — all seven are net-new backlog. Cheapest high-yield batch if scheduling a hygiene release: HYG-metrics-fields + HYG-discover-min-score + HYG-commits-found-flag + HYG-wal are each S-effort and independent; HYG-record-fail-rate's reason_rejected schema loosening rides along free. Two design-sensitive ones (dead-artifacts relocation, archive retrieval surface) deserve an owner decision before code, since both touch the 'legacy files are their own backup' and gitignored-archive invariants.
CHALLENGE HYG-record-fail-rate -> REVISED corrected:PARTIAL NOTE: Verdict and split stand; one fixed_in attribution is wrong. decision_errors (per-decision soft failure) landed in 1.19.0 (2026-04-29, commit 6faa9a8, CHANGELOG line 611), not '1.24.0/2.7.0'. 1.24.0 added finding_errors + summary truncation; 2.7.0 added resolve_errors (record-tools.ts:376-388). All still predate the field's 2.6.0 build for decisions/findings, so the residual-cause analysis is unchanged.
CHALLENGE HYG-archive-detectability -> REVISED corrected:PARTIAL NOTE: Verdict and fix_sketch stand, but the 'softening' of sub-claim 2 is overstated: the roll-marker finding is transient. It is an ordinary finding, so later sweeps archive it (partitionArchivable exempts only decisions and open need/warning/question — archiver.ts:67-81), and once in archive/*.jsonl it matches isArchiverLoopJunk's signature EXACTLY (archive-compactor.ts:43-64: finding + 'Archive: N entries archived' + tags ['archive'] + scope 'project' + agent_id 'main' + detail 'Archive summary: …' — precisely what archiver.ts:209-219/buildSummary emits), so compact_archives deletes legitimate post-1.24.0 markers as loop junk. Historical rolls therefore still degrade to git archaeology; treat fix_sketch (a) as required, and note the compactor signature cannot distinguish loop junk from legit markers — a small adjacent defect worth flagging.
CHALLENGE HYG-archived-visibility -> CONFIRMED NOTE: FIXED in 2.9.0 verified end-to-end, including tag membership: git merge-base --is-ancestor confirms both 017a878 (wave A, RETIRED_STATUS_DEBOOST in search.ts) and c1c46f7 (WHY_RETIRED_STATUSES in decisions.ts keyword path) are in v2.9.0 — the earlier 'git tag --contains | head' alphabetical sort (v2.10.0 before v2.9.0) is a trap, and I confirmed the ancestor check explicitly. The fallback_mode wart at decisions.ts:1301-1303 (zero-candidate status filter reported as fallback_mode: true) is real and correctly relegated to the risk note.

===== CLUSTER: hooks =====
--- HOOK-marker-path-scope [FIXED fixed_in: 2.15.0 / plugin 1.33.0 — commits 3758655 (edit-path filter), 8eb48f2 (canonicalized hook paths), 89842a2 (worktree-hosted TWINING_PROJECT stamping), all 2026-08-16] effort:S
CLAIM: The PostToolUse activity marker fired on ANY successful Edit/Write including /tmp scratch, so strictly read-only sessions taking out-of-tree notes got Gate-2 stop-blocked; report asks that the marker be scoped to paths under the resolved project root.
EVIDENCE: plugin/hooks/activity-marker-hook.sh:128-135 — 'Edit-path filter (2026-08-16): stamp only for edits that belong to THIS store's project'; absent/unparseable file_path deliberately falls through to stamping (fail toward gate integrity) | plugin/hooks/activity-marker-hook.sh:144 — relative paths covered: '[[ "$FILE_PATH" != /* ]] && FILE_PATH="$(pwd)/$FILE_PATH"' | plugin/hooks/activity-marker-hook.sh:55-63,145-147 — symlinked roots covered: canonpath() resolves through nearest existing ancestor with `pwd -P` (comment names /var→/private/var, symlinked homes; same mechanism covers /tmp→/private/tmp) and is applied to FILE_PATH, MARKER_SCOPE_ROOT, and MARKER_WT_ROOT
FIX: None needed in code. Residuals are deliberate, documented, and outside the report scenario: (a) unparseable/absent file_path stamps anyway — but all four matched tools always carry file_path/notebook_path; (b) Bash-driven edits never stamp (pre-existing design, gated at commit by pre-commit hook). Field machines only get this at plugin >=1.33.0 — the memo to the field (already an open memory item) should say so explicitly.
RISK: None at HEAD; field installs on plugin <=1.31.0 still exhibit the reported behavior until they update.
--- HOOK-fail-open-inventory [FIXED fixed_in: Design landed plugin 1.16.0 (#43, marker-based gate; CHANGELOG.md:362); never regressed — all branches verified byte-for-byte present at both v2.13.0 (their plugin 1.31.0 bundle) and HEAD] effort:S
CLAIM: The stop hook fails open on missing store, missing sentinel, missing session id, missing marker (named 'read-only session'), and carries a stop_hook_active loop guard — report asks each branch be verified real at HEAD, not just claimed in the header.
EVIDENCE: plugin/hooks/stop-hook.sh:26 — TWINING_DISABLED=true → exit 0 | plugin/hooks/stop-hook.sh:31-33 — loop guard: stop_hook_active:true → exit 0 | plugin/hooks/stop-hook.sh:85 — missing .twining store → exit 0
FIX: Nothing to fix — this was a verification item and the inventory checks out completely; the report's description of the hook is accurate. Verdict FIXED (not INVALID) because the claim is TRUE: the fail-open design shipped in plugin 1.16.0 and is intact at HEAD.
RISK: None.
--- HOOK-readonly-audit-path [PARTIAL fixed_in: Hook half only: 2.15.0/plugin 1.33.0 edit-path filter (commit 3758655) means out-of-tree note-taking no longer triggers Gate-2] effort:S
CLAIM: Combined with S4-14 (twining_status mutates the store on a fresh checkout), there is no supported read-only Twining audit path; with the marker fix landed, a documented read-only audit recipe should now be possible.
EVIDENCE: plugin/hooks/activity-marker-hook.sh:149-168 — hook half closed: /tmp notes no longer stamp, stop hook fails open on the missing marker (stop-hook.sh:104) | src/server.ts:53,64-66 — server half still open: 'Auto-creates .twining/ directory on first use'; createServer calls ensureInitialized(projectRoot) at boot, before any tool runs — merely connecting the MCP server in a fresh checkout writes the tree (src/storage/init.ts:75-100 mkdirs + config.yml + blackboard.jsonl). This is the S4-14 substance, owned by the backend-amnesia cluster; still true at HEAD | grep of README.md, docs/*.md, plugin/BEHAVIORS.md, docs/field-responses/*.md — no read-only audit recipe documented anywhere; TWINING_DISABLED is documented only as a commit-gate bypass (README.md:157,200)
FIX: Docs-first, two parts. (1) Rewrite docs/hooks.md: five hooks, marker-based Stop gate (per-session, epoch-marker vs .last-record), the 2.15.0 edit-path filter, and PostToolUse activity-marker section — removing the obsolete mtime description. (2) Add a 'Read-only audit' recipe to docs/hooks.md + README: run audit sessions with TWINING_DISABLED=true (disables all five hooks — note this also disables the pre-commit gate, session-start context, and subagent pending-posts, acceptable for a no-commit audit), take notes anywhere; on plugin >=1.33.0 even without the env var, out-of-tree notes never trigger Gate-2; the dashboard is the supported read-only viewing surface (README.md:351). Caveat in the recipe: on a FRESH checkout the server still creates .twining/ at boot — until S4-14 resolves (candidate: env-gated reuse of enterReadOnlyMode at src/server.ts:76, effort M, backend-amnesia cluster's call), a read-only audit is only fully clean against an already-initialized store.
RISK: Recommending TWINING_DISABLED could leak from audit sessions into normal workflow guidance and silently disable Gate 2 — the recipe must scope it explicitly to read-only audit sessions; the server-side read-only mode itself touches store bootstrap and belongs with S4-14, not this docs change.
NOTES: Item 4 (the withdrawn mtime/git-scan S2-E draft, report lines 513-527) was not classified per the report's own retraction and task instructions. Taxonomy note: HOOK-fail-open-inventory is a verification item, not a defect — verdict FIXED with fixed_in=plugin 1.16.0 is the closest fit for 'claim confirmed true, nothing open'; INVALID would wrongly imply the report was mistaken. Cross-cluster handoff: S4-14 (store mutation on fresh checkout) verified still present at HEAD at src/server.ts:66 / src/storage/init.ts:75-100 — the write happens at server BOOT, not specifically inside twining_status, which the backend-amnesia cluster may want for its root-cause framing. Discovered adjacent defect recorded under HOOK-readonly-audit-path: docs/hooks.md at HEAD still documents the pre-1.16 mtime Stop design and omits the activity-marker hook entirely — cheap fix, and plausibly the source of the audit readers' original misdiagnosis. CHANGELOG.md also has no 2.13.0-2.15.0 sections (top entry is 2.12.0) — noticed in passing, outside this cluster's scope.
CHALLENGE HOOK-marker-path-scope -> CONFIRMED NOTE: FIXED stands. Independently re-derived: filter present at HEAD and covers the report's exact scenario (/tmp notes canonicalize to /private/tmp, match no ALLOW pattern, never stamp; stop hook then fails open at stop-hook.sh:104). Checked for bypass: no branch sets TWINING_DIR without MARKER_SCOPE_ROOT (activity-marker-hook.sh:73,112,120), so the filter cannot be skipped when a store binds. v2.13.0 hook confirmed to have zero file_path handling (report accurate for audit build); commits 3758655/8eb48f2/89842a2 confirmed. Only unnamed micro-residual: the first-occurrence regex on raw hook JSON could theoretically match a 'file_path' literal inside edited content — pathological, outside report scope, does not change verdict.
CHALLENGE HOOK-fail-open-inventory -> CONFIRMED NOTE: FIXED stands. Every claimed branch independently verified at the exact cited line at HEAD, and git diff v2.13.0..HEAD on stop-hook.sh is empty — byte-identical at the audit's build, so no regression window exists. CHANGELOG Plugin [1.16.0] (2026-07-21, header at CHANGELOG.md:359, not 362 — immaterial) documents the #43 marker-based gate with the same fail-open list. FIXED is the right bucket for a verification item whose claim is true and whose design shipped in 1.16.0.
CHALLENGE HOOK-readonly-audit-path -> CONFIRMED NOTE: PARTIAL stands and the split is drawn correctly: hook half fixed (2.15.0 filter, verified in item 1), server half (S4-14) still open at HEAD, docs half fully open. Verified the server half myself: createServer → ensureInitialized runs at boot from src/index.ts:52, and init.ts writes the full tree on fresh checkout; even enterReadOnlyMode (server.ts:76) runs AFTER that write, so a fresh-checkout audit still mutates the store. Docs staleness confirmed exactly: docs/hooks.md:5 says 'four hooks' (hooks.json registers five), Stop section (line 18) still describes the pre-1.16.0 mtime design, activity-marker hook undocumented, and no read-only audit recipe exists anywhere in README/docs/BEHAVIORS.md. All five hooks honor TWINING_DISABLED (verified per file). Deferring S4-14's server fix to the backend-amnesia cluster is correct ownership, not a dropped defect.

===== CLUSTER: backlog-xref =====
--- 0a-publish-launcher [PARTIAL fixed_in: publish half: npm 2.15.0 (2026-08-16)] effort:M
CLAIM: Publish twining-mcp >=2.13.0 to npm, or make the launcher prefer the bundle — and note the bundle disables semantic search because it does not ship @huggingface/transformers.
EVIDENCE: orchestrator ground truth: npm latest is 2.15.0 (verified 2026-08-16) — the publish half is done | plugin/scripts/launch-server.sh:9-42 — rung order still override > pin > npx > npm-prefix > global > bundled; PKG_SPEC=twining-mcp@^2.0.0 (line 42), so a future npm lag reproduces the skew | scripts/build-plugin-bundle.mjs:80 — external: ['@huggingface/transformers', ...]; plugin/server/twining-server.mjs:32703 dynamic-imports it, so TWINING_SERVER_JS at the bundle still means fallback_mode keyword search
FIX: Either (a) pin PKG_SPEC to the bundle's exact version at plugin build time (bump-plugin-version.sh stamps it) so npx can never resolve older than the plugin ships, or (b) add a version-floor check that falls through to the bundle when npx resolves < bundle version; shipping transformers with the bundle is the heavier alternative. Also document that npx caches range specs.
RISK: Exact-pin breaks the 'npm hotfix reaches old plugins' property; bundle-first without transformers silently degrades semantic search — the report's own warning.
--- 0b-sqlite-state-size [OPEN] effort:S
CLAIM: hasSqliteState() must test size/valid sqlite header rather than existence, and ambiguous detection must fall back to files — a zero-length twining.db (or empty records/ over legacy decisions/) strands legacy-tier decisions unread.
EVIDENCE: src/storage/backend-resolve.ts:36 — `if (fs.existsSync(path.join(twiningDir, 'twining.db'))) return true;` — existence, not size (orchestrator-confirmed ground truth) | src/storage/backend-resolve.ts:37-39 — any file under records/ also counts as sqlite state; legacy decisions/ tier is only consulted after hasSqliteState fails | src/storage/backend-factory.ts:84-126 — explicit `backend: sqlite` never checks legacy content either, matching the report's 'not confined to resolveAutoBackend'
FIX: In hasSqliteState: require fs.statSync(twining.db).size > 0 (or read the 16-byte 'SQLite format 3' header); treat zero-length/unreadable as ambiguous and return false so the legacy-content check runs (module docblock lines 13-15 already states misdetection must land on files). Separately consider a startup guard: sqlite selected + empty db + non-empty legacy decisions/ => refuse-and-warn.
RISK: A legitimately fresh project that pre-created an empty twining.db would now resolve legacy/files; low — the docblock says that is the intended safe direction.
--- 0c-backend-warning [OPEN] effort:S
CLAIM: Invert the backend warning (failing path is silent, safe path warns) and surface the resolved backend/reason in tool output.
EVIDENCE: src/storage/backend-factory.ts:73-79 — console.error fires only when reason === 'legacy-content' (the safe files path); sqlite-with-stranded-legacy boots silently | src/tools/lifecycle-tools.ts:29-100 — twining_status output has counts/warnings but no backend or resolution-reason field | grep 'backend' across src/tools/*.ts — no tool response carries the backend anywhere
FIX: Thread StoreSet.backend + AutoResolution.reason (backend-factory.ts:49-62 already carries backend) into twining_status and assemble's result; add a boot-time warning when backend=sqlite and hasLegacyContent() is true (legacy tier present but unreadable by sqlite). The v1 index-desync sub-point: make files-backend getIndex reconcile against directory listing or at least count strays.
RISK: Additive fields — low; the boot warning must not fire on healthy migrated stores that legitimately retain old decisions/ files (check migration marker first).
--- T1-1-scope-segments [OPEN] effort:M
CLAIM: Match scopes on path-segment boundaries and return matched_via + directly_scoped_count so fabricated paths and bare-word scopes stop returning huge populations.
EVIDENCE: src/utils/scope.ts:7-9 — scopeMatches is still raw bidirectional String.startsWith, no segment boundary ('spec' matches 'specs/...') | src/engine/decisions.ts:704-766 — why result has total_in_scope/active_count but no matched_via or directly_scoped_count per row | grep matched_via/'segment' across docs and decisions mirror — untracked
FIX: scope.ts: normalize both sides with trailing '/' before prefix test (a===b || a.startsWith(b+'/') || b.startsWith(a+'/'), with '/'-stripped equality); add matched_via: 'exact'|'ancestor'|'descendant' and directly_scoped_count to why's response. scopeMatches is shared (why/assemble/triage/handoffs) so one edit fixes the class.
RISK: Field stores with sloppy scope strings ('src/auth' vs 'src/auth-api' accidental matches) lose matches by design — needs a memo note; regression tests across all four consumers.
--- T1-2-count-semantics [PARTIAL fixed_in: semantics fix + in-description doc: 2.9.0] effort:S
CLAIM: Add machine-readable count_semantics / schema_version to search responses so readers stop pinning docs to different generations of total_matched.
EVIDENCE: src/engine/decisions.ts:1261-1272 — total_matched is now an honest pre-page count with returned alongside (shipped 2.9.0, the generation skew's root cause) | src/tools/decision-tools.ts:364 — semantics are stated in the tool description prose, but no count_semantics/schema_version field exists in the response
FIX: Add count_semantics: 'pre_page_raw_floor' (and a response schema_version int) to searchDecisions' return in decisions.ts:1249-1273 and the SearchEngine result; bump when meaning changes.
RISK: None material — additive field.
--- T1-3-relevance-floor [PARTIAL fixed_in: reportable-absence core: 2.9.0] effort:S
CLAIM: Add above_threshold + a configurable relevance_floor and echo top_relevance so a zero-signal query is a reportable absence.
EVIDENCE: src/embeddings/search.ts:25-42 — semantic total_matched now counts only raw cosine above the ~0.3 noise floor, so a nonsense query reports total_matched near 0 (the reportable-absence outcome, shipped 2.9.0) | src/embeddings/search.ts:37-42 — the floor is a hardcoded constant, exactly what the report warns against; no config knob, no top_relevance echo, no separate above_threshold field
FIX: Promote the noise floor to config (search.relevance_floor, default 0.3) read in search.ts and the assemble admission floor; echo top_relevance (max raw score) on searchDecisions results. above_threshold equals total_matched in semantic mode — either alias it or add it for keyword mode clarity.
RISK: Config plumbing touches assemble's admission floor too — keep one shared source or the two floors drift.
--- T1-4-blackboard-excluded [OPEN] effort:S
CLAIM: Add blackboard_matches_excluded to search_decisions so the decision/blackboard tier boundary is visible.
EVIDENCE: src/tools/decision-tools.ts:360-415 — search_decisions searches only the decision store; no blackboard count in the response (grep blackboard_matches_excluded: zero hits) | src/tools/blackboard-tools.ts:133-183 — twining_query already runs both tiers in one call (the report's own section 7 notes it closes the hole), so the counting machinery exists
FIX: In the search_decisions handler, run engine.query's blackboard search with the same query, count hits above the shared floor, return blackboard_matches_excluded: n (count only — no rows).
RISK: Adds a second search per call (latency); keep it count-only and reuse embeddings cache.
--- T1-5-superseded-on-rows [OPEN] effort:S
CLAIM: Project superseded_by onto search rows and make status filtering array-valued or add exclude_status.
EVIDENCE: src/engine/decisions.ts:1250-1260,1320-1330 — search result rows carry status but not superseded_by; the description (decision-tools.ts:364) tells readers to 'follow superseded_by' which is absent from the row | src/tools/decision-tools.ts:377-379 — status filter is a single z.enum, no array, no exclude_status
FIX: Add `...(r.decision.superseded_by ? {superseded_by} : {})` to both mapping sites in decisions.ts (1320-1330 and 1383-1393); widen the status input to z.union([enum, z.array(enum)]) or add exclude_status: z.array(enum).
RISK: None material — additive; array status must stay backward compatible with the single-string form.
--- T1-6-metrics-fields [OPEN] effort:S
CLAIM: Add response_bytes + result_count to metrics.jsonl entries.
EVIDENCE: src/utils/types.ts:624-631 — MetricEntry is tool_name/timestamp/duration_ms/success/error_code/agent_id only | src/analytics/metrics-collector.ts — appends that shape; no size or count capture anywhere
FIX: Measure JSON.stringify(result).length at the toolResult wrapper (src/utils/errors.ts) or the metrics middleware and add response_bytes (+result_count where the result has an obvious array).
RISK: Metrics line growth trivial; result_count needs a per-tool convention or stays optional.
--- T1-7-assemble-disclosure [PARTIAL fixed_in: exclusion counts: 2.7.0 (archived), 2.9.0 (superseded)] effort:S
CLAIM: Port why's disclosure fields (total_in_scope, active_count, omitted_count, truncated) onto assemble.
EVIDENCE: src/utils/types.ts:200-219 — AssembledContext has warnings_omitted, archived_excluded_count, superseded_excluded_count (2.7.0/2.9.0) but no total_in_scope/omitted_count/truncated for the decisions lane | src/engine/context-assembler.ts:340-370 — decisions dropped for budget are silently skipped, no counter | src/engine/decisions.ts:754-766 — why already returns every field the ask names, so the shape is proven
FIX: Count scope-matched decisions before budget fill (the assembler already has the full list at context-assembler.ts:303); add total_in_scope, decisions_omitted, truncated to AssembledContext and the tool result in context-tools.ts:48-61.
RISK: None material — additive fields.
--- T1-8-marker-scope [FIXED fixed_in: 2.15.0 / plugin 1.33.0 (2026-08-16)] effort:S
CLAIM: Scope the activity marker to paths under the resolved project root so read-only sessions with /tmp scratch writes are not Gate-2 blocked.
EVIDENCE: plugin/hooks/activity-marker-hook.sh:128-169 — edit-path filter (dated 2026-08-16): stamps only for file_path under the canonicalized project root (or the session's own linked-worktree root); everything else exits 0 | plugin/hooks/activity-marker-hook.sh:55-63 — physical-path canonicalization so symlinked roots don't false-negative | git f92fced/3758655 — '2.15.0 — ... hook edit-path filter'; plugin 1.33.0 bundles it
--- T2-9-verified-empty [PARTIAL fixed_in: verified-empty half: 2.9.0] effort:S
CLAIM: Emit 'no decisions for this scope' only when the population is verified zero, and validate the scope so a nonexistent path returns scope_resolved: false.
EVIDENCE: src/engine/context-assembler.ts:671-696 — the empty briefing is now exclusion-aware: archived/superseded/omitted-warning counts force the full briefing with exclusion notes (2.9.0), so the false-empty half is fixed | no scope validation anywhere: src/tools/context-tools.ts:22-25 and decision-tools why accept any string; a fabricated path still assembles an imperative briefing from ancestor-scope matches (scope.ts:7-9)
FIX: At assemble/why entry, when the scope looks like a path, test existence against the project root and return scope_resolved: false (still deliver ancestor context, flagged). Must special-case logical scopes ('project', module labels) — flag only path-shaped scopes with a separator.
RISK: Scopes are not guaranteed to be fs paths; a wrong existence heuristic would false-flag logical scopes — flag, never suppress.
--- T2-10-continuework-status [PARTIAL fixed_in: age stamps + scopeless leak: 2.9.0] effort:M
CLAIM: Join continue-work items to live decision status at render time, or drop lifecycle words from them.
EVIDENCE: src/engine/context-assembler.ts:713-736 — handoff results still render stored status verbatim ('[BLOCKED Nd]'), no live join; age stamps shipped 2.9.0 mark staleness only | 2.9.0 scopeless-handoff fix (CHANGELOG.md:130-137) removed the leak that kept the field's fossil replaying everywhere, shrinking the class
FIX: Handoff results are free text, so a true join is not tractable; cheaper: render blocked items older than N days as '[BLOCKED Nd — unverified, re-check]' and stop echoing the bare lifecycle word, or resolve handoff-referenced decision ids (where present) against the store before rendering.
RISK: Free-text results mean any join is heuristic; mislabeling a genuinely blocked item as stale is the failure direction to avoid — wording change is the safe form.
--- T2-11-decisions-floor [OPEN] effort:S
CLAIM: Give decisions a reserved floor in assemble's budget or fill them before warnings, since the current order starves decisions.
EVIDENCE: src/engine/context-assembler.ts:303-320 — warnings still fill FIRST from the full budget; non-warnings (decisions included) are capped at 90% and take what remains | only a warning reserve exists (10%, line 307) — nothing reserves space for decisions
FIX: Mirror the warning reserve: cap warnings at e.g. 60% of budget when decisions exist in scope, or fill top-K decisions (the briefing's CRITICAL_COUNT=3) before the warning loop at line 320.
RISK: Warning-first was a deliberate design ('what not to do is the most actionable signal', line 703) — this is a re-balance, not a bug fix; needs a decision record and briefing-shape regression tests.
--- T2-12-token-estimate [OPEN] effort:M
CLAIM: Make token_estimate measure the serialized response and max_tokens bound the whole payload.
EVIDENCE: src/engine/context-assembler.ts:318-370,531 — token_estimate is the accumulated per-item estimate; briefing markdown overhead and planning_state ('not subject to token budget', line 550) sit outside it | src/engine/decisions.ts:715-732,765 — why's estimate likewise counts item JSON only; under saturation both read ~= max_tokens, the signature the report documents
FIX: After building the final tool result, set token_estimate = estimateTokens(JSON.stringify(response)) and, if over max_tokens, shrink (drop 'more' rows, truncate details) before returning — do it in the tool handlers (context-tools.ts:37-61, decision-tools why handler) where the final shape exists.
RISK: Iterative shrink can oscillate; keep one shrink pass and report truncated: true rather than looping.
--- T2-13-budget-knobs [OPEN] effort:M
CLAIM: Give every record-returning tool limit/max_tokens/token_estimate/truncated — export has no knob, read has no budget, neighbors has no limit, recent is huge at defaults, graph_query truncates silently.
EVIDENCE: src/tools/export-tools.ts:19-27 — export's only input is scope; no knob of any kind | src/tools/graph-tools.ts:109-127 — neighbors schema: entity/depth/relation_types, no limit | src/engine/graph.ts:179 — graph_query returns matches.slice(0, maxResults) with no total: silent truncation
FIX: Per sub-tool: graph_query — return total_matched before slice (one line at graph.ts:179); neighbors — add limit + truncated (fold into the tracked reduced-form work); export/read — add max_tokens with the what_changed summary-only shape the report itself endorses as the correct pattern.
RISK: export is used for handoff/archival where completeness matters — default must stay unbounded with an opt-in cap, not the reverse.
TRACKED: neighbors output shape only: 'D13 ask-2 neighbors work in its accepted reduced form' — docs/field-responses/2026-08-13-wave2-ship-memo.md:185-188 (also 2026-08-12-wave2-response.md:245); the budget knobs are NOT part of that item
--- T2-14-double-render [OPEN] effort:S
CLAIM: Stop double-rendering text: assemble warnings print summary and a detail that restates it; read's detail restates summary in most entries.
EVIDENCE: src/engine/context-assembler.ts:707-708 — warning renders `**summary**` plus detail unconditionally, no equality/near-dup guard | src/tools/blackboard-tools.ts:120-123 — read returns stored entries verbatim; detail duplication is data-level and unfiltered
FIX: At render (context-assembler.ts:708): skip detail when it equals or prefix-duplicates summary (normalized); optionally same guard in read's response mapping. The report's honest-accounting note says the read half is cosmetic — the render guard in assemble is the payoff.
RISK: A detail that legitimately begins with the summary then adds content must not be dropped — use full-equality or high-overlap test, not prefix alone.
--- T2-15-verify-clean-set [OPEN] effort:S
CLAIM: verify should list the clean set or report it with a stated basis, since passing decisions are never named.
EVIDENCE: src/engine/verify.ts:335-396,419-506 — checks report counts (decisions_checked, passed) and failing lists (stale, failed) but never name clean records | git log v2.6.0..HEAD on verify.ts — only wave-C tested_by stamping and D2 resolve changes; nothing addressed this
FIX: Add per-check passed_ids (id+summary, capped) or an explicit basis string ('checked N decisions with non-empty affected_files against git log since T') to each check result in verify.ts.
RISK: Payload growth on big scopes — cap the list and pair it with the T3 verify bounding work.
--- T2-16-triage-bykind-docs [PARTIAL] effort:S
CLAIM: Document triage's by_kind breakdown, since counts.open.by_kind.decision is the cheapest sound ratify-queue derivation and is undocumented.
EVIDENCE: docs/TRIAGE-SPEC.md:446-452,702 — by_kind shape and the fully-enumerated guarantee are specified in the repo spec | src/tools/triage-tools.ts:20-46 — the tool description (the surface agents actually read) documents counts.open.total pagination but never mentions by_kind or endorses by_kind.decision as the ratify count
FIX: One sentence in the twining_triage description: 'counts.open.by_kind.decision is the authoritative provisional (ratify-queue) count on every page' — and mirror it in the CLAUDE.md guidance the report's section 7 item 3 targets (which also notes twining_status.provisional_decisions as the unbreakable form).
RISK: None — docs only.
--- T2-17-discover-minscore [OPEN] effort:S
CLAIM: Give discover a non-zero default min_score so zero-overlap rows are not returned as matches.
EVIDENCE: src/engine/coordination.ts:164-166 — filter applies only `if (input.min_score !== undefined)`; default returns every scored row including zero-overlap | src/tools/coordination-tools.ts:148-159 — min_score is optional with no default in the schema
FIX: Default min_score to a small epsilon (exclude total_score === 0) in coordination.ts:164, document the default and how to restore old behavior (min_score: 0).
RISK: Behavior change for callers using discover as an agent roster — note it in the next memo's behavior-changes section.
--- T2-18-commits-found [OPEN] effort:S
CLAIM: Add found: bool to twining_commits (or error on an unknown hash) so a typo'd SHA is distinguishable from a real-but-unlinked commit.
EVIDENCE: src/engine/decisions.ts:881-904 — getByCommitHash returns bare {decisions:[...]} with no found flag and no hash validation | src/tools/decision-tools.ts:331-336 — tool passes through; empty array is byte-identical for typo and unlinked
FIX: Validate hash shape (7-40 hex) and return INVALID_INPUT on malformed; optionally verify existence via `git cat-file -e` when a repo is available and return commit_known: bool alongside decisions.
RISK: git-based existence check adds a process spawn and fails in bare/detached setups — make it best-effort with an 'unknown' third state.
--- T2-19-truncation-docs [OPEN] effort:S
CLAIM: Document the token_estimate ~= max_tokens truncation signature in the assemble tool description (docs-only, caller-fixable today).
EVIDENCE: src/tools/context-tools.ts:19-20 — assemble's description is two sentences about calling it first; no mention of max_tokens/token_estimate truncation semantics or that a large max_tokens yields an accurate decisions_count
FIX: Append to the description: 'token_estimate ~= max_tokens means the briefing was budget-truncated — re-call with a larger max_tokens for a complete decisions_count.' Same note in plugin/BEHAVIORS.md and the memo's CLAUDE.md guidance.
RISK: None — docs only.
--- T3-18-verify-bounds [DESIGN_DECISION_OPEN] effort:L
CLAIM: Bound verify (partial results, since watermark, max_items, published cost) and add a non-temporal content-hash signal so drift can see populated-but-untouched.
EVIDENCE: src/tools/verify-tools.ts:13-47 — inputs are scope/checks/agent_id/fail_on only: no since, no max_items, no partial mode | grep content_hash src/ — exists only for embedding reconciliation (storage/sqlite/db.ts:90), not as a decision-time code snapshot; drift remains git-mtime-temporal | report's own tier-3 heading (line 869): 'architectural, worth a decision rather than a patch'
FIX: Design decision: add since/max_items/partial to verify's contract, and capture a content hash of affected_files at record/amend time so drift compares content, not timestamps. The hash capture intersects the tracked diff-capture-at-record-time follow-up (ship memo:188-190) — decide them together.
RISK: Hash-at-record-time changes the write path and record schema; retroactive records have no baseline — the check must state its coverage honestly.
--- T3-19-mirror-authority [ALREADY_TRACKED fixed_in: visibility half: 2.14.0 (lifecycle_reverts) + 2.15.0 (blackboard warning)] effort:M
CLAIM: Resolve the mirror-vs-authority asymmetry (hand-repairs land on the copy that loses), plus the archive-threshold/reader mismatch and undetectable committed rolls.
EVIDENCE: docs/field-responses/2026-08-13-wave2-ship-memo.md:179-182 — 'file-wins ingest precedence for db-newer lifecycle state ... is an open design decision touching the W2.3 convergence invariant' — the named decision that governs which copy wins | 2.14.0/2.15.0 shipped the visibility half: lifecycle_reverts + blackboard warning naming reverted ids (ship memo:27-28) | archive-threshold sub-claim: src/tools/lifecycle-tools.ts:84-100 — HEAD deliberately counts the archivable partition, not raw entries, with recorded rationale; the reader-visible mismatch is by design, though committed-roll detectability remains unaddressed and untracked
FIX: Fold this report's S4-9 evidence (11-day phantom, hand-repair direction) into the file-wins precedence decision when it is taken; the roll-detectability point (git log --diff-filter=D) is a small docs addition that could ride any release.
RISK: Precedence flip touches the W2.3 convergence invariant — exactly why it is parked as a named decision.
TRACKED: file-wins ingest precedence design decision — 2026-08-13-wave2-ship-memo.md:179-182 (and :87-88)
--- T3-20-scope-semantics-docs [OPEN] effort:M
CLAIM: Publish per-tool scope semantics, since three tools return three different populations for one scope and none says why.
EVIDENCE: src/utils/scope.ts:1-9 — the shared predicate is documented only in a source docblock; per-tool population differences (why's affected_files fallback at decisions.ts:152-159 vs assemble's lanes vs summarize) appear in no published doc | grep docs/ TWINING-DESIGN-SPEC.md for per-tool scope population semantics — nothing beyond 'path-prefix semantics' one-liners
FIX: One docs page (docs/SCOPE-SEMANTICS.md or a DESIGN-SPEC section): the shared predicate, then per-tool population table (why = scope-match OR affected_files match; assemble = same plus lanes/budget; summarize = X), linked from each tool description. Best sequenced after T1-1 changes the predicate.
RISK: Documenting before T1-1 lands means writing it twice.
--- T3-21-why-affected-files [OPEN] effort:S
CLAIM: Expose affected_files on why output — the field the divergence-detection discipline keys on is not readable by any read-only tool.
EVIDENCE: src/engine/decisions.ts:717-728 — WhyDecision rows carry id/summary/rationale/confidence/status/timestamp/alternatives_count/commit_hashes/superseded_by — no affected_files | assemble's active_decisions DO carry affected_files (types.ts:225), so the asymmetry is why-specific | not in the ship memo's open follow-ups (:177-191) — untracked
FIX: Add affected_files (and affected_symbols) to the full WhyDecision at decisions.ts:717; keep them off the compact 'more' rows to protect the budget.
RISK: Payload growth on file-heavy records inside a budgeted response — the per-item cost estimate at line 729 already accounts for it automatically.
--- T3-22-graph-fate [ALREADY_TRACKED fixed_in: fate decision implicit in 2.11.0-2.15.0 investment] effort:S
CLAIM: Decide the graph's fate — surface it or retire it.
EVIDENCE: the decision is effectively made in favor of surfacing: 2.11.0 wave C (origin marker, upsert, populator isolation — CHANGELOG.md:19-53), 2.13.0 dedup pass, 2.15.0 indexed upsert all invest in the graph | remaining surfacing work is named: neighbors reduced form (edge-complete, hop depth, lifecycle awareness) — 2026-08-13-wave2-ship-memo.md:185-188; UNIQUE backstop — :182-183
FIX: If the report's S3-E evidence argues retirement, that would reopen a settled direction — the response doc should state the investment record and point at the named remaining items rather than re-litigate.
TRACKED: neighbors reduced form + UNIQUE(source,target,type) backstop — 2026-08-13-wave2-ship-memo.md:182-188
--- T3-23-record-invalid-input [DESIGN_DECISION_OPEN] effort:M
CLAIM: Reduce record's 10.3% INVALID_INPUT rate or return a repairable error.
EVIDENCE: src/engine/decisions.ts:290-319 and src/engine/blackboard.ts:105-124 — INVALID_INPUT messages already name the offending field ('domain is required', 'summary is required'), i.e. repairable in the naming sense; the RATE is a schema-ergonomics question | the 10.3% figure is field-store telemetry — not verifiable in this repo; tier-3 heading assigns the fix to a design decision
FIX: Ask the field for the error_code+message distribution from their metrics.jsonl (ride the memo's verification-questions channel); then decide between schema loosening (accept common near-miss shapes) and an error payload carrying the failed field + an example of the accepted shape.
RISK: Loosening the record schema trades validation strictness for rate — needs the distribution first, not a guess.
--- T3-24-agent-id [DESIGN_DECISION_OPEN] effort:M
CLAIM: Reconsider agent_id as a free-text field — 84.6% 'unknown', sometimes a whole task prompt, yet it keys verify's assembly join.
EVIDENCE: all tool schemas take agent_id as bare z.string().optional() (e.g. decision-tools.ts amend, context-tools.ts:30-33) — no format, no registry check | the project has already recorded the field's weakness as rationale: types.ts:243-245 / CHANGELOG 2.10.0 — 'agent_id cannot distinguish [sessions] — it is a role label' — but no identity-model decision is tracked | verify assembly check joins on it (verify-tools.ts:35-39 agent_id filter)
FIX: Design decision: either constrain (max length + slug pattern, server-side default from session identity like the 2.10.0 posted-id mechanism) or deprecate the field for joins in favor of session identity. The 2.10.0 self-post marking is prior art that identity can come from the process, not the caller.
RISK: Constraining breaks existing free-text writers; joins keyed on historical 'unknown' rows cannot be repaired retroactively.
--- MEMO-vehicle [OPEN] effort:S
CLAIM: Assess whether the unsent wave-2 ship memo is the right vehicle to also carry the response to this read-path audit report.
EVIDENCE: docs/field-responses/2026-08-13-wave2-ship-memo.md:1-31 — STATUS: LIVE, covers 2.8.0-2.15.0 cumulative upgrade, CLAUDE.md retirements, D14/D15 dispositions, open follow-ups; the two verification questions are at :58-62 (D14) and :75-80 (D15) | docs/field-responses/2026-08-16-memo-send.md — delivery wrapper is finished: retrieval commands, cover note calling the memo 'final', paste-ready field session prompt, expected response file | the report predates the wave-2 handoff back and most of its tier-0/instance findings dissolve on the upgrade the memo already instructs (2.6.0 -> 2.15.0)
FIX: Do not fold the audit response into the memo: the memo is scoped 'only what you need to act', is styled final, and has a clean archive condition (>=2.15 + retirements). Send it first/unchanged (minus the :183 stale-line fix) because its upgrade step resolves the report's 0a root cause and invalidates every 2.6.0-observed instance; then answer this report as a separate disposition doc (the 2026-08-12-wave2-response.md pattern) delivered through the same channel, referencing the memo for everything the upgrade already fixes and this triage for the surviving OPEN set. Note the report's own §7 operational warning: the report file is untracked on the audit machine — the cover note should ask them to commit it.
RISK: Bundling would blur the memo's archive condition and delay a send that is already the fix for the report's biggest class; sending without any acknowledgment of the new report risks the field re-measuring on 2.6.0 artifacts — the cover note should say the read-audit response is coming.
TRACKED: memo send itself is the tracked open action — project memory + 2026-08-16-memo-send.md
NOTES: Method: every verdict is from reading HEAD source (main @ 0a0aa89, 2.15.0) plus git log and docs/field-responses/; the decisions mirror (.twining/records/decisions/) was grepped read-only and corroborates the ship memo's tracked items (e.g. 01M04FDJ5G72T4YAHRYJC6KKTP file-wins revert detector). Duplicate report numbering: tier-3 restarts at 18, so tier-3 items carry T3- prefixes. Named open items located per instructions: file-wins precedence (ship memo:179-182), UNIQUE backstop (:182-183), per-decision supersedes (:183-185, also :118-121), neighbors reduced form (:185-188; wave2-response:245), diff capture (:188-190), lifecycle_reverts MCP-visible surface (shipped 2.15.0 — blackboard warning, ship memo:28), two verification questions (:58-62, :75-80; memo-send.md:66-73). Section 7 (report lines 954-973) was context, not classified per-item: its six CLAUDE.md corrections live in the FIELD repo's CLAUDE.md (the audit machine), not this repo — items 1-4 are consequences of asks T1-1/T1-2 plus D9-generation docs and are partly cured by the memo's CLAUDE.md retirements at upgrade; the response doc should carry the remaining corrections (why is prefix-match not scope-exact; by_kind.decision/status.provisional_decisions as ratify derivations; verify cost) as explicit field-CLAUDE.md edits, honoring the report's note that it wants an explicit decision. Discovered needs while triaging: (1) CHANGELOG.md's newest entry is 2.12.0 — 2.13.0-2.15.0 have no changelog entries, the ship memo table is currently the only release log for them; (2) ship memo line 183 still lists the sqlite relation-lookup index as open though line 28 records it shipped in 2.15.0 — fix before sending; (3) search_decisions' description tells readers to 'follow superseded_by' but the field is absent from search rows (T1-5) — a doc/shape mismatch introduced by the 2.9.0 description rewrite. Verdict tallies: 1 FIXED (T1-8), 3 ALREADY_TRACKED (T3-19, T3-22, plus MEMO-vehicle's send action), 6 PARTIAL, 3 DESIGN_DECISION_OPEN, remainder OPEN; nothing INVALID or FIELD_ENV_ONLY in this cluster (0a's npx-cache residue on the audit machine is field-env but the structural half is real, hence PARTIAL).
CHALLENGE 0a-publish-launcher -> CONFIRMED NOTE: One nuance the triage omitted without changing the verdict: the report's third sub-ask ("note the bundle disables semantic search") is already satisfied — the launcher emits that exact notice at both bundle-fallback paths (launch-server.sh:212, :242). The open remainder is solely the version-floor/skew-prevention mechanism.