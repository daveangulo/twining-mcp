# C03 — TODO (no implementing lane)

**Oracle:** [`../oracles/C03.oracle.md`](../oracles/C03.oracle.md) · held-out variant: [`../oracles/C03.heldout.oracle.md`](../oracles/C03.heldout.oracle.md)
**Subject:** C03 oracle — three consumers, three different prerequisites

## Status

**Not tested.** No executable test exists. This case has no implementing lane in
`docs/plans/2026-09-15-lane-briefs.md`, so lane 05 holds the stub.

This is a `not-tested` verdict and must be reported as such. It is not a pass,
not a waiver, and not "covered by review".

## Blocker

Lane 04's retrieval path (scope-first selection, the lifecycle resolver and the explain packet) is not merged. NOTE: docs/plans/2026-09-15-lane-briefs.md lists C03 inside lane 04's 'C01-C09' range while the lane-05 dispatch lists it as unowned. The lead must rule which it is before either lane writes the test.

## What to do when the blocker clears

1. Read the oracle and the held-out variant. Do **not** read the implementation
   first — the expectations are already fixed; the job is to discharge them, not
   to discover them.
2. Write `c03.test.ts` in this directory using
   `test/acceptance/harness/`. Declare the observable mapping in the file header.
3. Run the **development** fixture set first, then the held-out set. A different
   verdict between the two is a finding, not a fixture bug.
4. For every negative assertion, flip the control the oracle's
   "Instrument-can-fail controls" section names and prove the assertion fails.
   Use `harness/switchboard.ts`; add a control there if the oracle names one the
   switchboard does not carry yet.
5. Regenerate the index: `npx tsx scripts/qualify/build-oracle-index.ts`.

## Assertions this case states

See `test/acceptance/oracles/INDEX.md` — filter the "Every assertion id" table
to `C03`. Every one is currently `unowned` or `planned`.
