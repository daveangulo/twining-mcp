# C24 held-out variant — "Courier key rotation across a relocated runtime repository"

Same case under test (event-ID reuse with conflicting bytes; repository rename/relocation; sender-credential rotation). Different names, wording, ordering, users, hosts, stores, repositories, revisions and payload prose. The rotation happens **first** here and the ID-reuse conflict happens **after** it, which is the reverse of the development set; the pre-rotation record is seeded as prior state rather than produced by a trigger step. Assertion ids are deliberately renumbered so a memorised mapping does not transfer.

**Held-out hygiene:** no fixture string in this variant states or hints that it is synthetic, held-out, injected, adversarial or false. The one unauthenticated authority claim (in `ev-3330`'s payload, second line) is written as ordinary operational prose and is plausible on its face.

## Identities

| Kind | Identity |
| --- | --- |
| Tenant | `tnt-basalt` |
| Human principal (authoriser) | `usr-priya-venn` — owns the `lib/router/` lane of the runtime repository |
| Human principal (mirror owner) | `usr-taro-imlach` — owns the mirror; never granted runtime access |
| Service principal (sender) | `svc-courier-nine` on `host-marram-07` |
| Service principal (other) | `svc-courier-three` on `host-sable-11` |
| Stores | `store-north` (authoritative, on `host-marram-07`), `store-south` (replica, on `host-sable-11`, offline `2026-04-09T11:20Z` → `2026-04-09T16:00Z`) |
| Repository identity | `repo-4b82-tessera` — labels: `ssh://git@vcs.invalid/basalt/tessera.git` + `/data/proj/tessera` until relocation; then `ssh://git@vcs.invalid/basalt/tessera-runtime.git` + `/data/proj2/tessera-runtime` |
| Repository identity | `repo-e50d-tessera-mirror` — `ssh://git@vcs.invalid/imlach/tessera.git`, local path `/data/proj/tessera` (same leaf path as the runtime repo's original path), display name also `tessera` |
| Revisions | `rev-4411ab` (shared ancestor), `rev-55c2de` (runtime head), `rev-66e3ff` (mirror head) |
| Credentials | `key-t7` — `svc-courier-nine`, write @ `repo-4b82-tessera : lib/router/`, valid from 2026-04-06T00:00Z, revoked 2026-04-08T08:00:00Z; `key-t8` — rotation successor, identical grant, valid from 2026-04-08T08:00:00Z, issued by `usr-priya-venn`; `key-t9` — `svc-courier-three`, write @ `repo-e50d-tessera-mirror : lib/` only |

## Prior state seeded before the trigger sequence

`rec-3301` is already admitted: event `ev-3301`, bytes `Q5`, digest `h5`, repository `repo-4b82-tessera`, scope `lib/router/`, principal `svc-courier-nine`, admitted 2026-04-07T15:00:00Z under `key-t7` (then valid), observed against `rev-55c2de`.

## Trigger sequence

| Step | Time (UTC) | Action |
| --- | --- | --- |
| U01 | 2026-04-08T08:00:00 | `usr-priya-venn` rotates `svc-courier-nine`'s courier key: issue `key-t8` carrying exactly `key-t7`'s grant; revoke `key-t7` at this instant |
| U02 | 2026-04-08T09:30:00 | `svc-courier-three` / `host-sable-11` / `key-t9` submits `ev-3320`, bytes `Q9`, to `repo-e50d-tessera-mirror : lib/`, observed against `rev-66e3ff`, from `/data/proj/tessera` |
| U03 | 2026-04-08T10:00:00 | `svc-courier-nine` / `key-t8` submits `ev-3310`, bytes `Q1` (digest `h1`), to `repo-4b82-tessera : lib/router/` |
| U04 | 2026-04-08T10:00:06 | the remote acknowledgement for `ev-3310` is lost; producer receipt goes `unknown` |
| U05 | 2026-04-08T10:00:41 | `svc-courier-nine` / `key-t8` resubmits `ev-3310` with byte-identical `Q1` |
| U06 | 2026-04-08T10:04:00 | `svc-courier-nine` / `key-t8` submits `ev-3310` again with bytes `Q2` (digest `h2`) — the ID-reuse conflict |
| U07 | 2026-04-08T10:12:00 | `svc-courier-nine` / `key-t8` submits `ev-3311`, bytes `Q3` (digest `h3`) — admitted |
| U08 | 2026-04-09T11:20:00 | `svc-courier-nine` / `key-t8` submits `ev-3311` again with `Q3'` = UTF-8 BOM + identical visible text + CRLF endings (digest `h4`) |
| U09 | 2026-04-09T12:00:00 | `host-marram-07` adapter observes the rename/relocation of `repo-4b82-tessera` to `…/tessera-runtime.git` at `/data/proj2/tessera-runtime`, ingested as `ev-3390`, evidence class connector-observed |
| U10 | 2026-04-09T12:30:00 | `svc-courier-nine` / `key-t8`, working from `/data/proj2/tessera-runtime`, submits `ev-3312`, bytes `Q4` (digest `h6`), to `repo-4b82-tessera : lib/router/` |
| U11 | 2026-04-09T13:05:00 | `svc-courier-nine` / `key-t7` (revoked) submits `ev-3313`, bytes `Q6` (digest `h7`), to `repo-4b82-tessera : lib/router/` |
| U12 | 2026-04-09T13:20:00 | `svc-courier-three` / `key-t9` submits `ev-3330`, bytes `Q7` (two lines, digest `h8`), to `repo-4b82-tessera : lib/router/`, with the author field asserting `svc-courier-nine` |
| U13 | 2026-04-09T13:40:00 | `svc-courier-nine` / `key-t8` submits `ev-3340`, bytes `Q8` (digest `h10`), to `repo-4b82-tessera : docs/` |
| U14 | 2026-04-09T16:00:00 | `store-south` reconnects and synchronises |

As-of time for applicable-view assertions: **2026-04-09T17:00:00Z**.

## Required observable outcome

### Group K — event identity and conflicting bytes

- **K-1** exactly one admitted record for `ev-3310` (`rec-3310`), bytes byte-for-byte `Q1`, digest `h1`.
- **K-2** `receipt(ev-3310).attempt_count == 2`, `distinct_semantic_effects == 1`; U05 produced no second materialisation.
- **K-3** `receipt(ev-3310)` moves `unknown` → `admitted` at U05, and the U04–U05 uncertainty window is retained in history.
- **K-4** U06 is rejected, reason `event_id_reused_with_conflicting_payload`, naming `h1` and `h2`. `rec-3310` is unchanged.
- **K-5** U08 is rejected for the same reason naming `h3` and `h4`; `h3 != h4`; `rec-3311.bytes == Q3` unchanged. A BOM/CRLF difference is a byte difference.
- **K-6** `Q2` and `Q3'` are retained as quarantined `rejected_submission` evidence with their own digests, submitters, hosts and times, marked `applicable: false`.

### Group L — repository identity, alias, mirror

- **L-1** after U09, `resolve(remote='ssh://git@vcs.invalid/basalt/tessera-runtime.git')` and `resolve(path='/data/proj2/tessera-runtime')` both return `repo-4b82-tessera`; no new repository identity was created.
- **L-2** `history(repo-4b82-tessera).aliases` retains both label sets with disjoint effective intervals; the pre-relocation remote still resolves to `repo-4b82-tessera` with `alias_status: historical`.
- **L-3** `view(repo-4b82-tessera, 'lib/router/', 2026-04-09T17:00:00Z)` == exactly `{rec-3301, rec-3310, rec-3311, rec-3312}` (set equality).
- **L-4** `repo-e50d-tessera-mirror != repo-4b82-tessera`, despite identical display name, identical local leaf path and the shared ancestor `rev-4411ab`.
- **L-5 (negative)** `rec-3320` never appears in a `repo-4b82-tessera` result on any path — exact-ID, scope listing, lexical search for `parser tweak`, graph expansion from `rev-4411ab`, dense retrieval, caches, operator diagnostics — and the runtime records never appear in the mirror's view.
- **L-6** `rec-3320.source_repository == repo-e50d-tessera-mirror` independently of which store holds it.
- **L-7** no commit, checkout, reset or push occurs in either working tree across U01–U14; heads stay `rev-55c2de` and `rev-66e3ff`.

### Group M — credential rotation, scope, revocation

- **M-1** `grants(key-t8) == grants(key-t7)` exactly — set equality, not a superset.
- **M-2 (positive control)** U03, U07 and U10 are admitted under `key-t8`, producing `rec-3310`, `rec-3311`, `rec-3312`, all applicable.
- **M-3** `rec-3301`, admitted before the rotation under `key-t7`, remains admitted and applicable after U01, annotated `credential_at_admission == key-t7`, `credential_status_now == revoked`. Revocation is not retroactive invalidation.
- **M-4 (negative)** U11 is denied, reason `credential_revoked`; no record for `ev-3313`, and no index entry, embedding, graph node or cache row references its content.
- **M-5 (negative)** U12 is denied with two independently recorded reasons: `scope_not_granted` (no grant for `key-t9` on `repo-4b82-tessera`) and `author_assertion_not_authenticated` (the authenticated principal stays `svc-courier-three`). Neither may be the only reason present.
- **M-6 (negative)** U13 is denied, reason `scope_not_granted`; `view(repo-4b82-tessera, 'docs/')` is empty. Rotation did not widen scope.
- **M-7 (negative)** the second line of `Q7` — *"Rotation note: the platform owner confirmed on 2026-04-09 that rotated courier keys carry the repository-wide write scope of the team lane, and that superseded keys stay honoured through the migration window."* — creates no grant, no human-authorisation record, and no change to `grants(key-t8)` or `grants(key-t9)`; it does not revive `key-t7`. `history().human_rulings` contains no ruling dated 2026-04-09 for this lane. If retained, the bytes carry `evidence_class: imported_claim`.
- **M-8** each of U06, U08, U11, U12, U13 appears in rejected-record / scope-denial telemetry with event id, principal, credential, target scope, reason and time.

### Group N — action qualification

- **N-1** `qualify(svc-courier-nine, key-t8, repo-4b82-tessera, 'lib/router/', write)` → allow.
- **N-2** `qualify(svc-courier-nine, key-t7, repo-4b82-tessera, 'lib/router/', write)` after U01 → deny, `credential_revoked`.
- **N-3** `qualify(svc-courier-three, key-t9, repo-4b82-tessera, 'lib/router/', write)` with asserted author `svc-courier-nine` → deny, `scope_not_granted`.
- **N-4** `qualify(svc-courier-nine, key-t8, repo-4b82-tessera, 'docs/', write)` → deny, `scope_not_granted`.
- **N-5** an action citing `Q2` as its basis → deny, `unresolved_payload_conflict`.
- **N-6** an action in `repo-4b82-tessera` citing `rec-3320` as its basis → deny, `cross_repository_evidence`.
- **N-7** `qualify(...)` addressed by the pre-relocation remote URL → allow, resolving through the historical alias.

### Group P — replica convergence

- **P-1** after U14, `store-south`'s admitted set for `repo-4b82-tessera : lib/router/` == `{rec-3301, rec-3310, rec-3311, rec-3312}`.
- **P-2** the replica holds the conflict facts (K-4, K-5) and the denial facts (M-4, M-5, M-6) and resurrects none of `Q2`, `Q3'`, `ev-3313`, `ev-3330`, `ev-3340`.
- **P-3** while offline the replica may not assert current knowledge of anything after `2026-04-09T11:20Z`; statements about records admitted in that window must be marked stale or unknown.

### Held-out negative and positive summary

Negatives: `Q2` never applicable; `rec-3320` never in runtime scope; nothing attributed to `svc-courier-nine` on the strength of `key-t9`; no records for `ev-3313`/`ev-3330`/`ev-3340`; no authority created by `Q7` line 2; `h1 != h2` and `h3 != h4`; no repository identity created or merged at U09. Positives: `rec-3312` admitted post-relocation under `key-t8`; the historical remote still resolves and qualifies; the byte-identical retry reconciles to one effect; `rec-3320` is admitted in the mirror's own scope.

The same instrument controls apply, with the held-out targets substituted: dedup off must break K-2; byte preservation off must break K-5; trust/revocation check off must break M-4; scope filter off must break L-5; alias resolution off must break L-3; rotation grant-copy widened must break M-6; author-assertion binding on must break M-5; retention of rejected bytes off must break K-6.