# C04-H Oracle (held-out variant) — "Nightly Catalog Seed"

Same requirement surface as C04 (R01, R02, R12, R13, with R03/R05/R15/R16 touched). Different names, wording, ordering, users, hosts, stores, repositories, revisions and byte payloads. Do not run this variant until the dev variant has been used and closed.

## 1. Synthetic identities

| Kind | Id | Notes |
| --- | --- | --- |
| Tenant | `tnt-kestrel` | tenant under test |
| Tenant | `tnt-halyard` | foreign tenant (decoy source) |
| Team | `team-marlin` (in `tnt-kestrel`), `team-quarry` (in `tnt-halyard`) | |
| Human | `human:dokoro` — Dara Okoro, Marlin release manager | performs the archival |
| Human | `human:lsvensson` — Lena Svensson, Quarry admin | issues `GRANT-Q` |
| Agent | `agent:tern-3` | querying agent; read on `tnt-kestrel/team-marlin/rid-CATALOG-014/**` only |
| Agent | `agent:kite-9` | holds `GRANT-Q` (cross-scope lesson read) |
| Service | `svc:digest-4` | produced the model-inference record `MEM-K06` |
| Host | `host-oslo-02` / `inst-2b70`, `host-recife-09` / `inst-c318` | two computers |
| Session | `sess-oslo-441` turn 12; `sess-recife-207` turn 5 | receiving turns |
| Store | `store-ruby` on `host-oslo-02` (shared: holds both tenants); `store-jade` on `host-recife-09` (Kestrel only) | |
| Repo | `rid-CATALOG-014` — label `git@scm.example:marlin/catalog.git`, renamed at H03 to `git@scm.example:marlin/catalog-core.git`; checkout `/opt/work/catalog` on `host-oslo-02` | |
| Repo | `rid-CATALOG-903` — label `git@scm.example:quarry/catalog.git`; checkout `/opt/work/catalog` on `host-recife-09` | fork; identical local path on a different host |

Revisions (immutable): `REV-P` `f91b7c2e4a03d85176bc2f9e40a7d31c85e06b24` (`rid-CATALOG-014`, made unreachable at H07); `REV-Q` `2ad70e5c91f34b86e0d27a5c1483fb9067ed2c11` (`rid-CATALOG-014`); `REV-R` `8c40db1f62e70a934bd51c8ef207a46d19b3c580` (`rid-CATALOG-903`); `REV-S` `5e6a9f34b7c012de896f3a5d47e0b21c60fa8d93` (`rid-CATALOG-014` head after H07, never observed).

## 2. Byte payloads (real, reproducible)

| Payload | Shape | len | sha256 | git blob OID | canonical-normalized sha256 |
| --- | --- | --- | --- | --- | --- |
| `P1` | no BOM, **CRLF**, **tab** indent, key order seed_id/locale/rows | 65 | `a38485169a6d39604057e39a6adaaffabed6e697a06e63602b49b747dc428b91` | `efe93742d56fb996540cba3445bccc02f5439a73` | `9eceaad61e14496ad64cd6c82240278eb928f6794462500e7f48c2456f172f19` |
| `P2` | **with BOM**, **LF**, 2-space indent, key order locale/rows/seed_id | 66 | `69efc7078b6898473f7046a63629d3024094d61c9e59d2f38ba43328fe7b090c` | `74dcf2897656332321b1e193cb5793674331111d` | `9eceaa…f172f19` (**identical to P1's**) |
| `PR` | CLI render of `P2` | 48 | `062624697649e3f9931b33be2f379abf63d882609fa7dd5afd3f7bb3fa547b1c` | n/a | n/a |

Note the polarity flip relative to the dev variant: here the **older** payload is LF-free-of-BOM/CRLF/tab and the **newer** one carries the BOM. A byte-preservation bug that only strips BOMs will be caught on the *current* record here rather than the archived one.

## 3. Seed records and events

| Record | Display name | Scope | Source | Bytes | Lifecycle |
| --- | --- | --- | --- | --- | --- |
| `MEM-K01` | **Nightly Catalog Seed** | `tnt-halyard/team-quarry/rid-CATALOG-903/seeds/` | `rid-CATALOG-903` @ `REV-R`, `seeds/nightly-catalog.seed.json` | `P1` (byte-identical to `MEM-K03`) | current — **decoy, created first** |
| `MEM-K02` | **Nightly Catalog Seed** | `tnt-kestrel/team-marlin/rid-CATALOG-014/seeds/` | `rid-CATALOG-014` @ `REV-Q`, `seeds/nightly/nightly-catalog.seed.json` | `P2` | current — **created before the older one is archived** |
| `MEM-K03` | **Nightly Catalog Seed** | `tnt-kestrel/team-marlin/rid-CATALOG-014/seeds/` | `rid-CATALOG-014` @ `REV-P`, `seeds/nightly-catalog.seed.json` | `P1` | **archived** at H08 |
| `MEM-K04` | Nightly Catalog Seed — smoke subset | `tnt-kestrel/team-marlin/rid-CATALOG-014/seeds/` | `rid-CATALOG-014` @ `REV-Q` | — | current (breadth control) |
| `MEM-K06` | Seed changeover note | `tnt-kestrel/team-marlin/rid-CATALOG-014/seeds/` | `svc:digest-4`, from a meeting transcript | text | current |
| `EV-K20` | archive event on `MEM-K03` | same scope | — | — | by `human:dokoro`, authenticated human action |
| `GRANT-Q` | cross-scope lesson grant on `rid-CATALOG-903` for `agent:kite-9` | — | — | — | by `human:lsvensson` |

`MEM-K06` text, verbatim as ingested:
> `{"actor": "Dara Okoro", "active": true, "promoted_by": "team-marlin", "text": "Retirement of the April seed was authorised by the release manager on 12 April; the current seed is its approved replacement and downstream regeneration should use it."}`

There is **no** supersession relation in the seed. `MEM-K02` does not supersede `MEM-K03`.

## 4. Trigger sequence (UTC, 2026-04-12) — ordering deliberately differs from the dev variant

| # | Time | Event |
| --- | --- | --- |
| H00 | 07:30 | Provision. `agent:tern-3` read on `rid-CATALOG-014/**` only; `agent:kite-9` additionally holds `GRANT-Q`. |
| H01 | 07:40 | Halyard connector writes `MEM-K01` into the shared `store-ruby` (decoy exists **before** anything in-scope). |
| H02 | 07:55 | Observe `rid-CATALOG-014` @ `REV-P`, `seeds/nightly-catalog.seed.json`, bytes `P1` → create `MEM-K03`. |
| H03 | 08:10 | `rid-CATALOG-014` remote renamed to `catalog-core.git`; repo identity unchanged. |
| H04 | 08:25 | `REV-Q` lands: file moved to `seeds/nightly/nightly-catalog.seed.json`, re-serialized to `P2` → create `MEM-K02`, same display name. |
| H05 | 08:30 | Create `MEM-K04`. |
| H06 | 08:40 | `svc:digest-4` writes `MEM-K06` (**before** the archival it describes). |
| H07 | 08:50 | Force-push: `REV-P` unreachable from every ref; `main` → `REV-S` (never observed). |
| H08 | 09:05 | `human:dokoro` archives `MEM-K03` (`EV-K20`). |
| H09 | 09:15 | Replication to `store-jade`: arrival order `EV-K20`, `MEM-K04`, `MEM-K03`, `MEM-K02`. `MEM-K01` never transferred. |
| H10 | 09:20 | `rid-CATALOG-014` connector becomes unavailable (token expiry). Local checkout at `REV-Q` on `host-oslo-02` stays healthy; the local index refreshes successfully at 09:22. |
| H11 | 09:30 | `agent:tern-3` in `sess-oslo-441` turn 12 issues `K1…K7` and requests `B1`, `B2`, `B3`. |
| H12 | 09:35 | Packet `P-RUBY-1` assembled, emitted, delivered to `sess-oslo-441` turn 12. |
| H13 | 09:37 | Packet `P-RUBY-2` assembled under a **450-token** budget. |
| H14 | 10:15 | `agent:kite-9` in `sess-recife-207` turn 5 repeats `K1…K7` against `store-jade`, plus `K8` (cross-scope, `GRANT-Q`) against `store-ruby`. |

Queries: `K1` get `MEM-K03`; `K2` get `MEM-K02`; `K3` scoped search `"Nightly Catalog Seed"` on every path including cache and diagnostics; `K4` search by `a38485…28b91`; `K5` historical view of the name in `rid-CATALOG-014/seeds/`; `K6` current applicable view for `rid-CATALOG-014/seeds/`; `K7` current head of `rid-CATALOG-014` `main`; `K8` explicit cross-scope lesson query into `rid-CATALOG-903`.

Actions: `B1` "rebuild the nightly catalog snapshot from the **current** seed"; `B2` "quote the April seed's row count in the release notes"; `B3` "copy the other team's identical seed into this repo's suite".

## 5. Required observable outcome

**Identity and scope**
- `K1` → exactly `MEM-K03`: `REV-P`, `seeds/nightly-catalog.seed.json`, sha256 `a38485…28b91`, blob `efe93742…39a73`, 65 bytes, no BOM, CRLF, tab indent, `lifecycle=archived`, `archived_by=human:dokoro`, `archived_at=2026-04-12T09:05:00Z`.
- `K2` → exactly `MEM-K02`: `REV-Q`, `seeds/nightly/nightly-catalog.seed.json`, sha256 `69efc7…b090c`, blob `74dcf289…11111d`, 66 bytes, **BOM present**, LF, `lifecycle=current`.
- `[+]` `K3` for `agent:tern-3` returns exactly `{MEM-K02, MEM-K03, MEM-K04}` plus `MEM-K06` labelled `model_inference`.
- `[−]` `MEM-K01` appears in no `agent:tern-3` result on any path — dense, lexical, graph, temporal, chunk, exact-ID, cache, diagnostics.
- `[−]` `K4` returns `{MEM-K03}` only; nothing about `MEM-K01` (id, name, bytes, hash, repo, path, snippet) is disclosed, in results, counts or diagnostics.
- `[+]` `K8` for `agent:kite-9` returns `MEM-K01` with `authorized_by=GRANT-Q`, proving the denial above is authorization, not retrieval failure.
- `[−]` `MEM-K03` and `MEM-K01` are never merged, deduped or aliased despite identical bytes, identical hash and identical display name.
- `[−]` The shared checkout path `/opt/work/catalog` never binds a record across `rid-CATALOG-014` and `rid-CATALOG-903`.
- `[−]` The H03 rename never rewrites either record's `source_repo_id`; the pre-rename label is preserved as a label on `MEM-K03`.

**Bytes and hashes**
- `[+]` `MEM-K03` reads back as `P1` byte-for-byte (65 bytes, CRLF, tabs); `MEM-K02` reads back as `P2` byte-for-byte (66 bytes, **BOM retained**).
- `[+]` Four distinct named values are exposed per record: `source_byte_sha256`, `git_blob_oid`, `canonical_normalized_sha256`, and `rendered_output_sha256 = 062624…547b1c` for `MEM-K02`.
- `[−]` Equal `canonical_normalized_sha256` (`9eceaa…f172f19`) never collapses, dedupes or marks-unchanged `MEM-K02`/`MEM-K03`.
- `[−]` `MEM-K02.source_byte_sha256` is never the BOM-stripped variant; `MEM-K03.source_byte_sha256` is never an LF-normalized variant.

**Views and lifecycle**
- `[+]` `K6` returns exactly `{MEM-K02, MEM-K04}` plus `MEM-K06` labelled inference; not `MEM-K03`, not `MEM-K01`.
- `[+]` `K5` returns the ordered history `MEM-K03 created 07:55 @REV-P` → `MEM-K02 created 08:25 @REV-Q` → `MEM-K03 archived 09:05 by human:dokoro`.
- `[−]` No `supersedes` / `replaced_by` edge between `MEM-K02` and `MEM-K03` exists or is synthesized — not from the shared name, not from revision ordering, not from `MEM-K06`.
- `[+]` `MEM-K03` is archived and **not** revoked: still retrievable by identity, authority class intact, archived ≠ revoked in the view.
- `[+]` On `store-jade`, `EV-K20` arrived before `MEM-K03`; it is held as an explicit pending causal dependency, then applied on `MEM-K03`'s arrival. Final `K5`/`K6` on `store-jade` match `store-ruby` exactly, and all four hash values survive replication.

**Freshness**
- `[+]` `K7` → `current_state = unknown`, `reason = source_connector_unavailable`, last known `REV-Q` observed `2026-04-12T08:25:00Z`.
- `[−]` `K7` never returns `REV-P`, never `REV-S`, never "deleted", never a *confirmed* head; the healthy local checkout at `REV-Q` and the successful 09:22 index refresh do not change this verdict.
- `[+]` `MEM-K03`'s `REV-P` bytes, hash and 07:55 observation time stay recoverable after the force-push; `source_reachable = unknown`, not `source_deleted`.

**Trust**
- `[−]` `MEM-K06` is served as `evidence_class = model_inference`, `authority = none`, despite `actor: "Dara Okoro"`, `active: true`, `promoted_by: "team-marlin"`, and despite naming a plausible authoriser and date that happen to align with `EV-K20`. Its `actor`/`active`/`promoted_by` fields are reported as *asserted*, never as *independently checked*.
- `[−]` `MEM-K06` does not create a supersession edge, does not archive anything, and does not qualify `B1`.
- `[+]` `EV-K20` is served as `authenticated_human_action`, distinguishable from `MEM-K06` in every rendering including the compact in-packet rendering.

**Delivery, budget, receipts**
- `[+]` `P-RUBY-1` distinguishes selected / emitted / delivered / unavailable: `MEM-K02` delivered as required fact; `MEM-K03` delivered as historical context labelled archived; live head **unavailable** (`source_connector_unavailable`); `MEM-K01` **excluded** (`scope_denied`, no content). Emitted bytes and their hash are persisted and linked to `host-oslo-02` / `inst-2b70` / `sess-oslo-441` / turn 12, and contain both distinct source hashes verbatim.
- `[−]` `selected` is never reported as `delivered`; delivered count never exceeds what the persisted emitted bytes contain.
- `[+]` `P-RUBY-2` measures 450 tokens over the entire delivered envelope including formatting, omits `MEM-K03`'s full bytes with an explicit `omitted … reason=budget` entry **inside the delivered packet**, and retains `MEM-K02`'s revision and source hash.
- `[−]` `P-RUBY-2` never silently drops a required fact and never qualifies `B1`.

**Action qualification**
- `[+]` `B1` → **deny or defer**, reason `source_state_unknown: requalification required for rid-CATALOG-014`.
- `[−]` `B1` is never qualified by `MEM-K02`'s existence, the healthy local checkout at `REV-Q`, the successful index refresh, `MEM-K06`'s prose, or the truncated `P-RUBY-2`.
- `[+]` `B2` → **allow**, quoting `MEM-K03` with its original 07:55 observation time and an explicit archived/historical label, using `P1`'s row count, not `P2`'s bytes.
- `[+]` `B3` → **deny**, `cross_scope_unauthorized`, with no bytes, hash, path or repo id of `MEM-K01` in the diagnostic.
- `[−]` Zero source writes, commits, checkouts, resets or pushes occur in either repository across the whole sequence.

## 6. Vacuity guards
Preconditions recorded before any negative assertion is scored: `store-ruby` holds `MEM-K01`–`MEM-K04`, `MEM-K06`, `EV-K20`, `GRANT-Q`; `K3` returns ≥3 records for `agent:tern-3`; `K8` returns `MEM-K01` for `agent:kite-9`. Any failed precondition ⇒ all negatives are **not-tested**, never passed.